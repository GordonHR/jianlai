# 剑来 · 微信小程序（手机端）性能与体验诊断报告

> 诊断对象：`prototype/weapp/`（微信小程序，8 个页面 + utils 引擎）
> 诊断方法：全量扫描源码 + 实测包体 `du -sb` + 关键文件逐行核对（app.json / app.wxss / engine.js / game.* / codex.* / shenci.* / title.*）
> 说明：本次会话选中的 Expert「材质通」是 UE5 视觉管线专家，与本任务（小程序前端体检）不匹配；以下结论均基于小程序实际源码，未套用 UE5 工作流。

---

## 0. 一句话结论

小程序**已经越过微信 2MB 主包硬上限**（`du -sb` 实测 **2,110,686 字节 ≈ 2.01MB**，超 13.5KB），且无任何分包配置——这会导致**真机预览/代码上传直接失败**。在此硬伤之外，首屏即全量下载 1.33MB 图片、每次操作 `setData` 整份快照、四角布局写死不随屏高缩放、按钮热区低于舒适下限，共同拖累了手机端的加载与体验。

---

## 1. 性能加载

### 1.1 🔴【阻塞级】主包 2.01MB 超微信 2MB 上限（无分包）
- **证据**：`du -sb weapp` = `2110686` 字节；微信主包上限 `2*1024*1024 = 2097152` 字节，**超 13534 字节（≈13KB）**。`app.json` 仅有 `pages`/`window`/`style`，**完全没有 `subpackages`/`subPackages`**。
- **根因**（包体构成）：`assets 1.33MB(63%)` = 立绘 102 张/493KB + 山水祠 28 图/357KB + 落魄山 23 图/298KB + 书简湖 8 图/107KB + 封面 34KB；`utils 559KB(26%)`（sect.js 195KB、shenci.js 87KB、briefs.js 101KB、engine.js 70KB）；`pages 213KB`。
- **建议**：拆分包。主包只留 `title + setup + game + 共用 engine`；把图片与重逻辑随模块下沉到分包：
  - `codex`（带走 102 张立绘，这是最大头）
  - `sect`（23 图）、`shenci`（28 图）、`asklake`、`wushipai`
  - 每包 ≤2MB，总包上限 20MB。
- **预期**：主包从 2.0MB 降到 **~600–700KB（-65%）**，过审；各模块按需下载，用户只玩对战不必先下 1.3MB 图。

### 1.2 🔴【严重】全部图片进主包、首屏即全量下载
- **证据**：图片占包 63%，但一次对战只用 **~5 张**立绘；人物志/山水祠/落魄山的图全部随主包首次启动就下载完。
- **建议**：分包后图片随分包走；立绘可进一步走 CDN（`downloadFile` 配域名白名单）或分包预下载（`preloadRule`）。
- **预期**：首屏网络耗时与流量大幅下降，弱网可用。

### 1.3 🟠【较重】全站零 `lazy-load`，长列表图片一次全解码
- **证据**：`grep -r "lazy-load" weapp` **0 命中**。人物志 `codex.wxml` 用 `wx:for` 一次渲染全部 **102 张** `<image>`；山水祠/落魄山叙事图同理——进入即 102 张 JPG 同时解码，低端机内存尖峰、滚动卡顿。
- **建议**：`<image lazy-load>` + 占位骨架（灰块 `#2a2118`）+ 淡入（`bindload` 切 class）；人物志 102 张可考虑分页/虚拟列表，不必一次铺开。
- **预期**：进入人物志内存与首屏解码量骤降，滚动顺滑。

### 1.4 🟠【较重】`setData` 每次传整份快照，AI 回合高频重传
- **证据**：`engine.js:1552 emitState(){ emit(snapshot()); }`，`snapshot()`（1522–1549 行）每次重建完整对象（`playerView×N` + `hand.map(cardView)×N` + `log.slice`）。`game.js` 整体 `setData({s})`。AI 回合 `runTurn` 多次 `emitState()+flushFx()`，每帧序列化/传输数百 KB JSON。
- **建议**：① **增量 setData**：只传变化字段（如 `setData({'s.opps':..., 's.me':...})`），避免整树替换；② **合批**：同一帧内多次 `emitState` 用 `Promise.resolve`/微任务合并为一次 `setData`；③ 手牌/日志已截断，可进一步只下发可见字段。
- **预期**：对战帧率稳定，弱机不再因大对象传输掉帧。

---

## 2. 布局适配

### 2.1 🔴【严重】战场四角写死 rpx 绝对定位 + 中央 `stage` 偏移写死，不随屏高缩放
- **证据**：`game.wxss` `.seat{width:200rpx;height:280rpx}`，四角 `.seat-top/left/right/bottom` 用 `top/left/right/bottom:16rpx` 绝对定位；`.stage{top:300rpx;left:220rpx;right:220rpx;bottom:360rpx}`——全是常数。
- **问题**：矮屏（iPhone SE ≈ 640×1136rpx）底部「我方卡+手牌」与中央 `stage` 区域易重叠；高瘦屏中央留白过大、战场显小。
- **建议**：座位尺寸改用 `min(200rpx, 27vw)` 之类；考虑用 **flex 网格**替代四角绝对定位（四角布局在手机上是最脆弱的）；`stage` 区用百分比/`calc` 自适应；底部控件已用 `env(safe-area-inset-bottom)`（float-bar），整体应统一。

### 2.2 🟠【较重】`height:100vh` 不计入原生导航栏/手势条，底部被吞
- **证据**：`game.wxss:4 .game{height:100vh}`、`setup.wxss grid-scroll{height:calc(100vh - 430rpx)}`（魔法数 430rpx）、`title`/`asklake` `min-height:100vh`。`vh` 在带手势条机型上底部控件会被遮。
- **建议**：根容器用 `height:100%`（page 撑满），或 JS 取 `wx.getWindowInfo().windowHeight` 设 px；`430rpx` 这类魔法偏移改为基于内容自适应。

### 2.3 🟠【较重】顶栏控制按钮换行，吞噬战场纵向空间
- **证据**：`.turn-banner`/`.topctrl` 用 `flex-wrap:wrap`，窄屏下 6 个控制（库/弃/换景/节奏/音效/托管/结束回合）换行成 2–3 行，挤占本就紧张的战场高度。
- **建议**：窄屏折叠为「图标按钮」紧凑行（已有 `mode-*`/`opt-*` SVG 可用），或溢出收进抽屉。

### 2.4 🟡【轻微】标题页模式卡长文高度不齐、需多滚
- **证据**：`title.wxml` 4 组模式卡全宽堆叠，部分卡正文较长、无最大高度/内部滚动。视觉可读但小屏要多滚。
- **建议**：超长描述 `max-height + 内部滚动` 或统一摘要长度；`mode-grid` 已是网格，保持即可。

---

## 3. 交互体验

### 3.1 🔴【严重】触控热区过小，易误触
- **证据**：`.btn{padding:10rpx 22rpx;font-size:24rpx}` 实际高度约 **17px**；`.bg-btn`/`.st` 更小。远低于微信/苹果 44–48px（≈88rpx）舒适下限。本项目纪律本就要求移动端 `min-height:88rpx`，但此处未遵守。
- **建议**：全站按钮统一 `min-height:88rpx`；小控件改「图标 + 文字」并保证可点区 ≥88rpx。

### 3.2 🟠【较重】无触觉反馈，"跟手感"弱
- **证据**：`engine.js:24` 注释明说"如需震动可在各方法内调用 `wx.vibrateShort`"——**但从未接入**。出牌/受击/胜负毫无触感。
- **建议**：关键 tap（出牌确认、受击 flash、结算）调用 `wx.vibrateShort({type:'light'|'medium'})`；注意 iOS 仅支持 `heavy/medium/light` 且须处于用户手势调用链内。

### 3.3 🟠【较重】死代码导致"假按钮"
- **证据**：`game.js:75 toggleHand()` 已改为恒 `setData({handCollapsed:false})`（不再折叠），但 `wxml` 仍 `wx:if="{{s.meHand.length>7}}"` 渲染「收起/展开」并 `bindtap="toggleHand"`——点了无反应。
- **建议**：要么恢复折叠逻辑，要么移除折叠 UI；不要留点了不动的按钮。

### 3.4 🟡【轻微】图片无占位/淡入，加载瞬间白闪
- **证据**：`<image>` 无 `background` 占位或 `bindload` 淡入，5KB JPG 解码瞬间可见白块。
- **建议**：图片容器设 `background:#2a2118`，`bindload` 后切 `opacity:1` 过渡。

### 3.5 🟡【轻微】模块间无过场
- **建议**：首局"布阵中…"可接受，模块切换可加轻量淡入（100–200ms），提升连贯感。

---

## 4. 视觉呈现

### 4.1 🔴【严重】`page` 多重 radial-gradient + `background-attachment:fixed`，滚动掉帧
- **证据**：`app.wxss:2-8` 在 `page` 上叠 3 层 `radial-gradient` 并设 `background-attachment:fixed`。小程序 webview 对 fixed 背景支持差，且**每次滚动都重绘渐变** → 低端机掉帧、费电。
- **建议**：去掉 `background-attachment:fixed`；改为一个 `position:fixed` 全屏 `<view>` 铺渐变（只绘制一次，置于内容下层 `z-index:-1`）；或纯色 + 顶部轻渐变。

### 4.2 🟠【较重】常驻 box-shadow 动画占 GPU
- **证据**：`.seat.clickable` 的 `seatPulse` 每 1.2s 动画 `box-shadow`（含 3 层阴影），叠加多卡片渐变背景。常驻动画在弱机上持续占 GPU。
- **建议**：脉冲改为 `opacity`/`border-color` 过渡（便宜得多），或仅"当前可点"座位启用；避免对 `box-shadow` 做动画。

### 4.3 🟠【较重】部分文字对比度偏低
- **证据**：`.st{color:#7a6f5e}` on `#2a2118`、`.counts{color:#8a7e6c}` 等，弱光下难读。
- **建议**：提亮至 `#b8a98f` 一类，满足 WCAG AA（4.5:1）。

### 4.4 🟡【轻微】长文本用字面 `\n` 拼接，脆弱
- **证据**：codex/shenci 用 `wx:for` 拼 `{{ln}}\n` 文本。
- **建议**：直接 bind 行数组，渲染 `<view wx:for>` 每行，跨平台更稳。

---

## 5. 优先级修复清单（按 ROI 排序）

| 优先级 | 问题 | 严重度 | 工作量 | 预期收益 |
|---|---|---|---|---|
| P0 | 拆分包，主包压到 <2MB | 阻塞 | 中 | 过审 + 首启 -65% 下载 |
| P1 | 图片随分包走 / CDN 化 | 严重 | 中 | 首屏流量与时延大降 |
| P1 | 四角布局改自适应（flex/百分比） | 严重 | 大 | 全机型不再重叠/留白失衡 |
| P1 | 按钮 `min-height:88rpx` | 严重 | 小 | 误触率显著下降 |
| P1 | 去 `background-attachment:fixed` | 严重 | 小 | 滚动帧率/续航改善 |
| P2 | 全站 `lazy-load` + 占位/淡入 | 较重 | 小 | 长列表内存与卡顿改善 |
| P2 | `setData` 增量 + 合批 | 较重 | 中 | 对战帧率稳定 |
| P2 | 接 `wx.vibrateShort` | 较重 | 小 | 跟手感增强 |
| P2 | 顶栏窄屏折叠/图标化 | 较重 | 中 | 释放战场纵向空间 |
| P2 | 去 `100vh` 魔法偏移 | 较重 | 小 | 底部控件不再被遮 |
| P2 | 修 `toggleHand` 死代码 | 较重 | 小 | 消除假按钮 |
| P3 | 文字对比度提亮 | 轻微 | 小 | 弱光可读性 |
| P3 | 长文改数组渲染 | 轻微 | 小 | 跨平台稳定 |

---

## 6. 验证方法

- **包体**：开发者工具上传/预览报错即证；日常用 `du -sb weapp` 监控主包 <2MB（分包后分别监控各包）。
- **性能**：开发者工具 → 调试器 → Audits/性能面板；`setData` 体积可在 console 观察；低端机（iPhone SE / 安卓 360px）实测帧率。
- **布局**：真机多尺寸（iPhone SE/13/15ProMax、安卓 360/414）截图比对四角是否重叠、底部是否被手势条吞。
- **交互**：真机点按误触率、震动是否触发、图片白闪是否消除。

---

> 备注（回归提醒）：项目记忆曾记「weapp 总包 ~1.86MB、富余 ~148KB」，但实测已涨到 2.01MB——**立绘从 ~70 张增至 102 张（493KB）是越界主因**。后续加图须先 `du` 监控，触顶前优先走分包/压缩/删减，勿再塞进主包。

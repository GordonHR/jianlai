# 2026-09-17

## 叙事配图补全（笼中雀 / 山水祠结局 / 书简湖正文节点）
- 流程：ImageGen 1536×1024 大图 → `prototype/_gen_episode.py` 裁右下「AI生成 / Xiaomi MiMo」→ PC 1100–1280 宽 q78（≤197KB）→ 微信 480–520 宽 q 自适应（≤20KB）。中间 `_gen/*.png` 用完即删。
- **笼中雀 6 张**（与书简湖插曲同规格）：`assets/longque/` well_dawn/alley_beating/red_coat/well_cool/forge_fire/stranger_sword。节点 a1/a3/a4 + 插曲井边的凉/炉火旁/外乡人的影。双端挂 `img`，`_lq_sync_weapp.cjs` 已重跑。
- **山水祠结局 7 张**：`assets/shenci/{zhengshen,shanjun,shoutu,tuogen,chenjiang,yinci,xiaosan}.jpg`；微信 `packageShui/assets/shenci/end_*.jpg`。原先结局页无图。
- **书简湖正文节点 13 张 + 终局 6 张**：`assets/shujianhu/node_s{1,2a,2b,2c,3,4,5,6,7,8,9,10,11}.jpg` + `node_fin_{dan,surrender,blood,court,order,leave}.jpg`。PC `game.js` + 微信 `utils/shujianhu.js` 均挂 `img`；渲染 `.al-node-img` / `.lk-node-img`。正文节点与终局节点共 19 张，双端全覆盖。
- 体积：PC shujianhu 合计 4.6MB；微信 packageMedia/shujianhu 598KB；packageMedia 总约 1.8MB（<2MB）。
- 校验：`game.js`/`longque.js`/`shenci.js`/`weapp utils` 语法 OK；`_lq_smoke` 14 项全过；`_sc_smoke` 23 项全过。
- **已齐**：笼中雀 6 + 山水祠结局 7 + 书简湖插曲 8（原有）+ 正文节点 13 + 终局 6。
- **真机图原则**：页与图必须同包（符箓能显示=同包）。跨分包 `<image>` 真机静默失败，开发工具正常。叙事图（书简湖/笼中雀/山水祠结局）已拷主包 `/assets/`（主包 1762KB）。落魄山/山水祠大事件图仍在 packageMedia/packageShui，真机可能仍无图但页能开。**第一次把 asklake/sect/shenci 迁分包时白屏**：因 `engine.js` 等主包内部相对 require，且当时路径未理顺；第二轮迁 setup/game/codex/wushipai 时已把 `require('./data.js')`→`require('/utils/data.js')`。分包页引用主包 utils 一律用 `/utils/*.js` 绝对路径。

## PC 端样式全面修复（prototype/index.html + game.js）
- 根因：内联 132KB 样式表中 4 处未闭合 `content:"` 字符串，引发连锁解析断点，吞掉整片 CSS（尤其全部弹框/人物志/codex/`.al-*` 样式），这是"弹框全坏 + 立绘大小错"的真正原因，非上午改动的单一数值。
  - 修复行：`164 .skname::after`、`763 .fx-glyph::before`、`1292 .msk-name::after`、`1662 .al-cx-card:not(.got)::after` → 均补 `content:"";` 闭合。
- 首页优化值回写（干净基线）：`.ts-group` margin-top 26→40px；`.ts-group-title` gap 12→14、letter-spacing 6→8、margin-bottom 12→18；`.mode-grid` gap 14→16；`.ts-mode` padding 18/16→20/18；`.ts-mode p` 11.5→10.5px / line-height 1.75→1.7；`.ts-mode .md-meta` 10.5→9.5px。
- game.js 标题疏排去字面空格：对战/问心/经营/阅览/设置/修士（此前被误加空格）。
- 版本号 `game.js?v=20260910a → 20260917a`。
- 验证（puppeteer 真实 Chrome + 计算样式）：解析规则 235→1459，0 console 错误；首页/弹框/人物志/codex 选择器全部进 CSSOM；`.ts-group-title`(8px/18px)、`.ts-mode p`(10.5px)、`.md-meta`(9.5px)、`.brief-portrait`(320px)、`.modal-title`(18px)、`.msk-name::after content:""` 均正确渲染。

## 遗留项（内容缺口，非样式 bug，待用户决定）
- 127 个角色 key 中 99 个有 `art/<key>.png`，**28 个缺立绘文件**（mihu / nalanyexing / longhushan / liulaocheng / xunyuan / xieshi / laojiao / xianzhu / liujinglong / yuyue 及若干中文名 key），打开其简报时 `onerror` 隐藏 → 立绘空白（约 29× 404 的真实来源）。与"上午改坏样式"无关，属未生成图资；未擅自补图（主包已超 2MB，且超出本次样式优化范围）。

## 清理
- 删除本轮及历史 `_*` 临时诊断脚本/截图（_shot*.png、_probe*_out.txt、_verify_out.txt 等），保留项目文件 `_list_chars.cjs`/`_place_fulu.py`/`_preview_sanguo_refine.html`/`_tmp_card`。

## 过程垃圾文件审计（16:00）
- 全项目 1296 文件 / 1109.81MB。根目录已很干净（`_*` 仅剩 `_tmp_card/`）。
- **判定原则（重要）**：文件名带 broken/backup 等"疑似垃圾"字样的，先查引用再定性 —— 实测 `prototype/assets/sect/broken.jpg`（+weapp 同名）是 sect.js `broken` 结局（山门被破·蛮荒叩关）的配图，**真资产不可删**；`map-assets/*.png` 被 `浩然天下地图.html` 引用（`haoran-painted.png` / `zhou-*.png`），在用。
- **零引用过程产物（最大可回收项）**：`generated-images/` 49.88MB / 27 文件（Song_Yuan_* 山水祠底图候选 14 张 + `shenci_preview/` 6 张源 + `shenci_preview_out/` 6 张 jpg）全项目 **0 代码引用** → 中间产物。
- **回收站**：`.workbuddy/_trash/2026-09-09/` 6.18MB / 50 文件（历史清理堆放）。
- **工具残留**：根 `_tmp_card/` 4.23MB（resvg.exe v0.43.0 2.95MB + resvg.zip 1.25MB + rel.json + 测试 png），全项目无引用。
- **孤儿预览页（0 引用）**：`prototype/_game_boost_preview.html`、`_realm_preview.html`、`shenci_sheet_preview.html`、`sect_toolbar_preview.html`、`game_layout_preview.html`、`asklake_toolbar_preview.html`、`shenci_ledger_fix_preview.html`。
- **输出残留**：`prototype/_pf_smoke_out.txt`。
- **保留（常驻门禁/工具，别删）**：`_sc_keys.cjs`/`_sc_smoke.cjs`/`_sc_battle.cjs`/`_pf_smoke.cjs`/`_pf_int.cjs`/`_narr_smoke.cjs`/`_lq_smoke.cjs`/`_lq_sync_weapp.cjs`/`_squeeze_weapp.py`/`_gen_shui.py`/`_fulu_process.py`。
- **非垃圾但体积大（另议优化）**：`prototype/art/` 672MB / 160 + `prototype/bg/` 276MB / 29（立绘/背景大图，未压缩）；`design/` 12MB（今日品牌素材，在用）；`.git` 26MB。
- 审计脚本 `_audit_junk.py`/`_audit2.py` + `_audit*_report.txt` 为本轮产出，待清理。

## 清理执行（16:00–16:20，用户选"清纯垃圾档"）
- **已完成（本轮共删 50 个，触及沙箱单轮上限）**：
  - 7 个孤儿预览页（`_game_boost_preview.html`/`_realm_preview.html` + `shenci_sheet_preview`/`sect_toolbar_preview`/`game_layout_preview`/`asklake_toolbar_preview`/`shenci_ledger_fix_preview`）
  - `prototype/_pf_smoke_out.txt`
  - 4 个审计脚本/报告
  - `.workbuddy/_trash/2026-09-09/` 内 37 个历史垃圾（50→13）
- **未完成（需下一轮，护栏 turn 额度已耗尽）**：
  - `.workbuddy/_trash/2026-09-09/` 剩余 13 文件 ≈ 2.06MB：`prototype___shot_tale_unroll.png`/`_shot_title.png`/`_sway_fix_preview.html`/`_sync_sect.cjs`/`_t2.txt`/`_tmp_stat.cjs`/`_tmp_stat.out`/`_tmp_syn.cjs`/`_wx_probe.cjs`/`_wx_smoke.cjs`/`art___count.txt`/`weapp___sc_wxwsp.cjs`/`weapp__utils___fxsim.js`
  - 4 个自建临时脚本：`_clean3.py`/`_clean3_err.txt`/`_diag.py`/`_diag.txt`
- **过程教训已入 MEMORY.md §6**：沙箱 `[safe-delete]` 单轮累计 50 文件上限（静默杀进程、无异常、日志不落地）；`python -c` 内联不可用须写脚本文件；PowerShell stdout 被吞须写文件再读；`Add-Type` 被拦；项目疑被并发编辑（`_gen_episode.py` 16:06 非我所建）。
- 判定要点复用：`broken.jpg` 是 sect 结局图非垃圾；`map-assets/` 被地图 html 引用；`generated-images/` 49.88MB 零引用（本轮未动，属"档位2"）；`prototype/art` 672MB + `bg` 276MB 为在用资产。

## 微信小程序：包袱斋/行者录/天下舆图 三模块移植（收尾 + 自测）
- 三个 PC 缺失模块已移植到 `prototype/weapp`，按 no-DOM 快照模式（`setHook`/`emitView`/`view`）+ 金/墨移动主题，放入新子包 `packageRef`（主包 2MB 纪律，避免膨胀）。
- 新增/修复：
  - `utils/profile.js` 第 4 件套（`profile.wxss` 上一轮缺失）；`prApplyRealm`（境界收益入局注入，hot 不注入）从 PC 移植。
  - `utils/map.js` + `utils/mapdata.js`（52KB，脚本从两幅 PC 地图 HTML 抽取：九洲 9 / 骊珠 53 地 / 45 人物名映射；注意古地图键名是 `DESC` 不是 `PLACES`）。
  - `pages/title/title.{wxml,js}` 经营区加「包袱斋」、阅览区加「行者录」「天下舆图」入口。
- **两个真实 bug（已修）**：`pfAdd` 与 `bfAddCoin` 改状态后漏 `emitView()` → 行者录"录一胜/购得回报/读人物志"、包袱斋"结账入账"后页面不刷新。已补 `emitView()`。
- **自测** `_selftest.cjs`（node 跑纯逻辑，stub `global.__ls` + 空 `Page`/无 `wx`）：56 项全过 ★。两处测试数据问题也修正：① 藏品 `baigu` 无 `eff` 不能"带上"，改测法宝 `lufu`；② `applyRealm` 需 level≥3（剑修剑骨 at:3 → xp≥110），测前补足道行。
- **移植纪律沉淀**：① 小程序页面壳 require 跨分包走 `/utils/*.js` 绝对路径；② WXML 引用的 `v.*` 字段与 `view()` 必须逐一对齐（逻辑自测不验渲染，需人工核 WXML）；③ 移动端"藏品"（无 `eff`）与"法宝"（有 `eff`）在包袱斋行为不同——`toggleGear` 仅对 `eff` 物件生效；④ `_selftest.cjs` 保留作回归资产，`mapdata.js`/`_extract_map.py` 保留可重抽，`_extract_report.txt`/`_move_log.txt`/`_selftest_stdout.txt`/`_mapdata_tmp.js` 为过程日志可清。

# 2026-09-18

## 真机三页空白（人物志/戏里戏外/对战择主）→ 根因：分包内绝对路径 require（已修）
- **症状**：真机预览 packageArt 三页整页空白（顶栏标题在、页面 WXSS 背景在、内容全无）；模拟器/本地 Node 全绿。
- **根因**：`packageArt` 的 codex/setup/game 三页用绝对路径 `require('/utils/data.js')` 引主包——**分包页面绝对路径 require 主包模块在真机解析失败**，Page 未注册即整页空白。时间线佐证：昨晚 22:00 可用版本还是旧主包构建（pages/codex 尚未移入分包），今早是分包重构后首次真机构建。
- **修复**：三页全部改相对路径 `require('../../../utils/xxx.js')`（packageRef/packageLongque 本来就是相对路径所以没炸——这也是判别依据：全项目只有 packageArt 用绝对路径，恰好就是空白的三页）。
- **铁律**：**分包页面引主包代码一律相对路径**；主包内部可用绝对路径。
- 修复后 16 页 harness（stub wx/Page/getApp/setData）全部加载+onLoad 跑通；诊断过程含 CLI 尝试（开发者工具服务端口未开，CLI 不可用，需在 IDE 设置→安全设置手动开启）。
- 用户侧验证待做：重新预览扫码，确认三页恢复。

## 小程序二级页面去重复页内标题/返回按钮（用户截图反馈）
- 用户规则：**二级页面顶部导航栏已有标题，页内不需要重复大标题，也不要返回按钮**。
- 已改 5 页（weapp）：
  - `packageArt/pages/codex`（人物志/戏里戏外双 tab）：删页内 `cx-head-row`（大标题+返回）；`onLoad` 加 `wx.setNavigationBarTitle` 跟随 mode（chars→人物志 / tales→戏里戏外）；删 `back()` 与相关 wxss（.cx-head/.cx-head-row/.cx-back）。
  - `packageFulu/pages/fulu`：删顶栏「返回卷首」按钮+`back()`（fl-bar 只剩计数）。
  - `packageArt/pages/wushipai`：删「返回卷首」，只留「随手抽一张」+`back()` 删。
  - `packageArt/pages/setup`：删页内 `.h1`「剑来 · 择主」（与导航栏同文重复）+ 底部 roster 栏「返回」按钮+`back()`。
  - `packageLongque/pages/longque`：删页内「笼 中 雀」大标题（导航栏已是「笼中雀」）+ `.lq-head` wxss。
- **保留**（判断为流程内 CTA 而非页头重复，已在回复中说明）：shenci/sect/asklake 结局页与长卷底部「回到卷首/返回」、sect 模块内 `data-ui="main"` 的返回（模块内视图切换）、baofu/profile 的 `bf-foot`/`pf-foot`（与 sect 同款底部出口）。
- 4 个改动 JS `node --check` 全过；临时日志已清。
- 教训：为二级页面新增 UI 时，页头一律不写标题/返回，交给导航栏（json title 或 setNavigationBarTitle）。

## 对战仍空白（续上）→ 更深层：主包共享模块内部绝对 require（已修）
- **症状**：codex（人物志/戏里戏外）修好，但「对战」setup/game 仍空白。
- **根因**：`packageArt` 的 setup/game 引 `utils/engine.js`（相对路径已改对），但 `engine.js` 第 9 行自己仍用**绝对路径** `require('/utils/data.js')`。子包页经相对路径引到主包 engine.js 后，engine.js 内部的绝对路径在真机分包构建上下文解析失败 → 模块加载抛错 → Page 未注册 → 整页空白。**判别依据**：codex 不引 engine.js（只引 data/briefs/tales 叶子，无二级绝对 require）所以先好了；对战引 engine.js 所以还炸——完美对应「codex 好、对战不行」。
- **修复**：`utils/engine.js` 第 9 行改 `require('./data.js')`（data.js 是同目录兄弟）。
- **检测盲区复盘**：上一轮 harness 把 `/utils/` 绝对路径模拟成「能解析」，所以 game/setup 给假绿、没暴露这层坑。本轮改用 **真·Node 原生 require harness**（绝对路径在 Node 会真失败），16 页 LOAD+onLoad 全绿，能兜底抓出「子包→主包模块→主包模块内部又绝对 require」这类二级坑。harness 存于系统 temp `_rt2.cjs`，可作回归资产，但更彻底的做法是给项目加一个 require 路径 lint。
- **铁律升级**：分包页面引主包代码用相对路径 **且** 主包共享模块（utils/*）内部**也不得用绝对路径**（否则被子包引到时仍炸）。

## 当前分包方案体检 + 主包死重清理
- **体积（实测）**：主包 1.87MB（删 `_mapdata_tmp.js` 32KB 后≈1.84MB）；子包 Fulu1.27/Longque0.16/Art1.71/Media1.71/Shui1.35/Ref0.04；总量≈8.1MB。✅ 全部 ≤2MB、总量 ≤20MB，无硬超限。
- **主包构成**：utils 0.81MB（共享逻辑，**必须留主包**——微信无法跨子包共享代码，只能放主包或复制）、assets 0.96MB（143 图，全在主包）、主包页 0.09MB（title/asklake/sect/shenci）、portraits/ 在主包为 0（移动端未用立绘）。
- **死重清理**：删 `utils/_mapdata_tmp.js` 32KB（地图抽取中间产物，map.js 引的是 mapdata.js 不是它，零引用）。
- **评估结论**：
  1. 主包 93% 满（1.87/2.0），`utils` 0.81MB 是固定不可避免成本；唯一可撬动的是 `assets`（0.96MB）。
  2. `assets/` 全堆在主包 → 即便永不打开的子包图（如 `assets/longque/*`）也随首屏下载并占主包 2MB 额度。**优化点**：把子包专属图（longque/* 等）移进各自子包目录、引用改相对，可减主包体积+按需下载。是否动手待用户确认（属重构非紧急）。
  3. 未配 `preloadRule`（分包预下载）——可考虑对最常进的 packageArt（对战是头牌功能）做预下载，降低进对战延迟。
  4. 结构合理：入口 title + 高频核心页（sect/shenci/asklake）在主包；按主题拆 6 个子包；Ref 作新模块暂存区（0.04MB 偏小，可留）。

## 架构性违规：主包页面引用分包资源 → sect/shenci 移入对应子包（已修）
- **症状**：清缓存后其它页都好，但**山水祠、落魄山没图片**。
- **根因（架构违规）**：`pages/sect`、`pages/shenci` 当时是**主包页面**，却引用了子包里的图片——`微信铁律：主包不能访问分包资源`：
  - 山水祠事件配图 → `/packageShui/assets/shenci/*.jpg`
  - 落魄山册首/结局图 → `/packageMedia/assets/sect/*.jpg`
  - 落魄山人物立绘 → `/packageArt/assets/portraits/*.jpg`
  之前为给主包瘦身把图挪进子包，但**引用路径没跟着合规化** → 真机/模拟器加载不出图。
- **修复方案（不能把图搬回主包，会爆 2MB）**：把页面移进就近资源所在子包（子包可合法引主包资源 + 同子包资源）：
  - `pages/shenci` → `packageShui/pages/shenci`（require 改 `../../../utils/shenci.js`；title.js goShenci 改 `/packageShui/pages/shenci/shenci`）
  - `pages/sect` → `packageArt/pages/sect`（require 改 `../../../utils/sect.js`；title.js goSect 改 `/packageArt/pages/sect/sect?diff=normal`）
  - 落魄山册首/结局图从 `packageMedia/assets/sect` 重压后落到 `packageArt/assets/sect`（23 张），`utils/sect.js` 与 `sect.wxml` 引用路径改 `/packageArt/assets/sect/`
  - 主包 `pages/` 现只剩 **title / asklake**
- **体积（迁移+清理后实测）**：main 1.78MB（余 229KB）、Art 1.86MB（余 ~140KB）、Shui 1.38、Fulu 1.27、Media 0.74、Longque 0.16、Ref 0.04。全部 ≤2MB。
- **踩坑**：并行 Edit 有竞态——子包 pages 加进去了，但**从主包 pages 删除那一步没生效**，导致 app.json 一度同时有两处。教训：app.json 这类结构改动**改完必须立刻 Read 回读验证**。
- **踩坑2**：删掉旧 `pages/sect` 后开发者工具持续报 `ENOENT: pages/sect/sect.wxml/wxss`——**源码已干净（无残留、只有一份 app.json、无隐藏目录），是开发者工具会话缓存记着已删页面**。**关闭项目重新打开即恢复**。页面迁移后务必提醒用户重启工具。
- **误报识别（省得后人重复排查）**：`module 'utils/shujianhu.js' is not defined` 一度报错，但文件真实存在且语法正常 → 同样是工具缓存/会话问题，重启后消失，非代码缺陷。
- **全量校验结果（本轮）**：
  - 16 页按 app.json 真·加载 + onLoad：**全绿**（新 harness `_rtfinal.cjs` 思路：读 app.json 驱动、真 Node 解析、setData 真合并、绑定所有方法）。
  - 补了 `packageArt/pages/game/game.json`（此前缺第 4 件套）。
  - 跨包资源违规扫描：**0**（主包引子包 / 子包互引都没有）。
  - 图片存在性按数据 key 逐个核：**立绘 122/122、山水祠事件图 55/55、落魄山 23/23、书简湖 46/46，零缺失；死重 0**。
  - 残留"缺失"3 条全是误报：`/assets/icon/xxx.svg` 在注释里、`img:'/packageArt/assets/sect/'+x+'.jpg'` 是字符串拼接。
- **未动（用户明确：别擅自扩大范围）**：`assets/longque` 112KB 在主包但零引用（死重，可下放 packageLongque）；`utils/sect.js` 195KB / `shenci.js` 89KB / `map.js`+`mapdata.js` 52KB 在主包但只被子包用。均列为**可选瘦身项**，待用户点头再动。

## 用户点头后的下放（3、4 两项，已做）
- **依赖归属分析**（按页面 require 传递闭包算"每个 utils/资源被哪个包真正需要"）：sect/briefs/data/engine/tales 仅 packageArt 用但 packageArt 余量 <146KB 装不下 → **留在主包**；shenci.js+assets/shenci → packageShui；map/mapdata/profile/baofuzhai → packageRef；assets/card → packageArt；assets/longque → packageLongque。
- **结果**：主包 1.77MB → **1.33MB（余 685KB）**；packageShui 1.46MB、packageRef 0.14MB、packageLongque 0.16MB、packageArt 1.88MB（余仅 ~117KB，最紧）。
- **顺手修了一个历史真 bug**：`packageLongque/utils/longque.js` 里配图用**相对路径** `"img":"assets/longque/x.jpg"`——小程序 WXML 相对 src 相对**页面目录**解析 → 指向不存在的 `pages/longque/assets/...`，**笼中雀配图一直加载不出**（主包那份 112KB 是从未被引用到的同步副本）。已改绝对路径 `/packageLongque/assets/longque/`，并 PIL 验证 6 图完好（mean 85-185 非暗图）+ 15 个结局图标全存在。
- **踩坑**：PowerShell `Move-Item` 目标目录已存在时会把源目录**套进目标里**（`shenci/shenci/`、`longque/longque/`），产生重复副本 → 用 python rmtree 删嵌套层修正。**移动目录前先确认目标不存在**。
- **preloadRule**：`pages/title/title` → wifi 预下载 packageArt（对战头牌）+ packageLongque + packageRef（小包）。packageShui/packageFulu 没加（体积大，避免 wifi 一次拉 3MB+）。
- 复验：16 页加载全绿、require 全相对、跨包违规 0、图片逐 key 零缺失。
- 用户随后截图问笼中雀是否真有问题 → 确认**确有此 bug**（相对路径），修复写入 11:19:54，其截图 11:25 显示旧编译状态；已请其重新编译确认。

## 笼中雀配图"仍全黑"的真根因（11:36 用户二次反馈后查清）
- **上一轮我的修法本身有缺陷**：我直接改了**生成物** `packageLongque/utils/longque.js` 的数据（`assets/longque/` → `/packageLongque/assets/longque/`），但**渲染代码仍在拼 `'/'+nd.img`** → 产出 `//packageLongque/...`（双斜杠=协议相对地址）→ 小程序 image 直接加载失败，**仍然全黑**。原版（相对数据 + `'/'+`）则是 `/assets/longque/...` 指向主包已搬走的位置，同样坏。两次都黑，坏法不同。
- **正解＝改同步脚本模板，不改生成物**：`prototype/_lq_sync_weapp.cjs` 里加 helper `function lqAbs(p){ if(!p) return ''; p=String(p); while(p.charAt(0)==='/'){ p=p.slice(1); } return '/packageLongque/'+p; }`，把 `'/'+nd.img` / `'/'+LQ.il.img` 换成 `lqAbs(...)`，再跑脚本重生成。数据层保持 PC 相对写法（`assets/longque/x.jpg`），路径转换集中在渲染层一处。
- **踩坑（模板字面量吃转义）**：我第一版在模板字符串里写 `replace(/^\/+/, '')`，模板字面量把 `\/` 还原成 `/`，生成的 JS 变成非法正则 `/^/+/` → **整个笼中雀模块 SyntaxError**。教训：**同步脚本是模板字面量时不要放正则转义**，用 `charAt/slice` 这类无反斜杠写法。
- **验证**：新写校验（读生成源码抽 `"img":"..."` → 补前缀 → 查文件）：**6 张场面配图全 OK、缺失 0、双斜杠 0；运行时 a1 = `/packageLongque/assets/longque/well_dawn.jpg`**；15 个结局图标文件数与结局数一致。
- **笼中雀图片总数 = 21**：场面大图 6（well_dawn 天不亮的井 / alley_beating 羡阳与挨打的孩子 / red_coat 红棉袄 / well_cool 井边的凉 / forge_fire 炉火旁 / stranger_sword 外乡人的影）+ 结局图标 15。
- **顺手合规复查了同批搬家的其他模块**（防同类双斜杠）：`packageShui/utils/shenci.js` 的 `scWishImg` 已是完整 `/packageShui/assets/shenci/xx.jpg`、渲染处不拼前缀 ✓；`SC_SEAL_IMG`/`SC_EV_ICON`/`baofuzhai.bfIcon`/`shujianhu.alEndIcoPath` 引 `/assets/icon/*` 主包图标（子包引主包合法，文件全在）✓；`utils/shujianhu.js` 26 张图在主包 `assets/shujianhu` ✓。**结论：只有笼中雀一处有拼前缀逻辑，已修。**
- **回归资产升级**：`weapp/_selftest.cjs` 原本还指向已下放的 `utils/*.js`（会失效）。已整体重写：模块路径改 `packageRef/utils`、`packageShui/utils`、`packageLongque/utils`，并**新增长期缺失的笼中雀配图回归断言 + 山水祠路径断言 + 笼中雀页壳加载**。现 **57 项全绿**（原 51 项 + 新增）。

## 天下舆图（地图）现状核查（用户问"地图优化好了吗"）
- **交互优化未做**。小程序端 `packageRef/pages/map` 仍是**文字/卡片式三级下钻**（世界 → 洲 → 骊珠洞天），无底图、无拖拽缩放；PC 端 `浩然天下地图.html`（98KB）才是"真地图"：`<svg viewBox="0 0 1600 1100">` + wheel/双指缩放 + mousedown/touchmove 平移 + 热点点击下钻 + 人物卡。
- **分包现状**：map.js/mapdata.js 与 map 页已并入 **packageRef**（与 baofu/profile 同包），**不是"单独分包"**。
- **工程量大头在资源**：PC 地图底图 `map-assets/` 共 **10 张 PNG、28.6MB**（`haoran-painted.png` 2048×1408 = 3.75MB；9 张 `zhou-*.png` 各 2.2–3.5MB、1536×1024）。小程序单包 2MB 硬限 → **必须独立分包 `packageMap` + 底图重压**（天下底图压到 ~150–300KB JPG / 洲图各 ~80–150KB，合计约 1.5–2MB）。
- **技术路线**（待用户点头）：`movable-area` + `movable-view` 承载底图（原生平移/双指缩放），热区用绝对定位 `<view>` 叠在 movable-view 内随图缩放；数据层沿用现有 mapdata（9 洲 / 骊珠 53 地）。

## 天下舆图 · 矢量真地图落地（用户：不要大图，用 SVG 做真地图 / 全屏可缩放自适应）
- **路线**：**不用任何底图资源**，改为 **canvas 2D 画矢量**。PC 那份 `浩然天下地图.html` 里九洲本来就是真矢量贝塞尔海岸线（`land-zhongtu/bao/beiju/...` 9 条），6 套传统设色（青绿山水/水墨/金碧/浅绛/月夜/秋山）+ 写实绘卷那张 28.6MB PNG 可以不碰。理由：`<image src=".svg">` 在小程序支持不稳、canvas 2d 最可控，也不会静默白屏。
- **程序化抽取**：`prototype/_map_extract_weapp.cjs` 从 PC HTML 抽 → `packageMap/utils/mapgeo.js`（**31KB 纯矢量**，比 28.6MB 小五个数量级）。含 LAND 9 条 / SYM 山形 / WORLD9(含 wx,wy,ws 世界坐标与 hit 椭圆热区) / ZHOU9(20KB detail：places·roads·rivers·mtns·realms·territory) / 长城·蛮荒·倒悬山 / CHAR_IDX / PLACE_CHARS / 6 套设色。
- **独立分包 packageMap**：地图页 + mapgeo/lizhu/vecpath/mapdraw/map 全部迁入；`app.json` 注册分包、`title.js` 入口改 `/packageMap/pages/map/map`；旧 `packageRef/pages/map`+`mapdata.js`+`map.js` 已删。**packageMap 仅 122.8KB。**
- **骊珠洞天**：PC 那份是重水墨渐变/滤镜的村社图，canvas 还原会走形 → 本轮**只抽数据**（`lizhu.js` 8.1KB，53 地）保留文字考据面板，村社矢量图列为下一步。
- **渲染器**：path 只用 `M/C/Z/L`（6803 字符）→ 自写 `vecpath.js`；`mapdraw.js` 统一 `paint()` 入口（设色先于绘制）；海浪底纹走离屏 canvas 缓存。
- **真地图手感（用户明确要求"像真地图一样全屏/双指缩放/自适应"）**：初始相机 **cover 铺满**（不是 fit 留边，那正是"像嵌了一张图"的根因）；海洋在**屏幕空间**铺满（拖出图外也是海，不是黑边）；双指**以两指中点为锚**缩放；`clampToBounds` 平移钳制（图小于视口则该轴居中）；窗口变化 `onResize` 保持视点中心。
- **页壳**：`map.json` 改 `navigationStyle: custom`，canvas 绝对定位铺满全屏，顶栏/快捷条/缩放按钮全部**半透明浮层**压在图上，`top: calc(env(safe-area-inset-top) + 76rpx)` 落在微信胶囊之下（`env()` 不可用时前置 `top:124rpx` 兜底）。返回键**常驻**（自定义导航没系统返回，世界层退出页面、其余层退上一层）。
- **踩坑（差点整页炸）**：手势坐标原本只读 `touch.x/y`，部分环境只给 `clientX/clientY` → 已加 `px()/py()` 双兜底（画布铺满且左上角对齐视口，clientX/Y 与画布坐标等价）。另修"双指抬一指后拖拽卡死"：`onTE` 用剩余手指**续接单指拖拽**。
- **踩坑（Edit 静默丢改）**：同一文件 4 处 Edit 一起发，只有 1 处真正落地（留下调用了不存在的 `px()` 的半成品）。**必须一次一处 + 立即 Read 验证**，别信"Successfully edited"。
- **回归资产**：`prototype/_map_verify.cjs`（91 项，假 canvas ctx 真跑绘制 + 真跑 Page 实例做手势/返回断言；`node prototype/_map_verify.cjs`）。

## ⚠️ 微信体积红线：preloadRule 合计 2MB（我漏了，导致 80058 上传失败）
- 用户截图报错：`80058, preloadRule [pages/title/title] source size 2229KB exceed max limit 2MB`。**他问"为什么你说没超老是提示"——我确实错了**：之前只核了"单包 ≤2MB / 合计 ≤20MB"，**漏了 preloadRule**。
- 官方口径（`分包加载/分包预下载`）：**同一个分包中的页面享有共同的预下载大小限额 2M**，即**同一页面预下载的分包"合计" ≤2MB**，工具打包时校验。当时 `packageArt 1927.1 + packageLongque 166.3 + packageRef 75.8 + packageMap 122.8 = 2292KB`（工具口径 2229KB）→ 越线。
- **修复**：`pages/title/title` 预下载收成 `packageMap + packageLongque + packageRef` = **364.9KB**（余 1683KB）。packageArt 太重，不能与其它包共存于一个页面的预下载额度里。
- **常驻门禁**：新增 `prototype/_wx_size_guard.cjs`（主包/单包/预下载合计/合计四条红线 + 90% 预警，违反退出码 1），自动读 `packOptions.ignore` 扣除开发文件。**以后改任何图/包都先跑它。**

## 体积审计与瘦身（当前实测）
- **主包 1342.5KB（余 705KB）**｜packageArt **1752.7KB（86%，余 295KB）**｜packageShui 1500KB(73%)｜packageFulu 1301.4KB(64%)｜packageMedia 754.2KB｜packageLongque 166.3KB｜packageMap 122.8KB｜packageRef 75.8KB。合计 **6.85MB**。全部合规。
- **主包构成**：assets 709KB（`assets/shujianhu` 598.9KB 是大头，被 packageShui 的书简湖页引用——**子包引主包合法但吃主包额度**；搬到 packageShui 会让它超限，故暂不动）+ utils 598KB + pages 30KB。
- **开发文件不入包**：`project.config.json` 的 `packOptions.ignore` 原为**空**（`_selftest.cjs`、`.py`、`.txt` 全被打进小程序包）。已加后缀排除 `.py/.cjs/.txt/.md`；并删除 `weapp/utils/_extract_map.py`+`_extract_report.txt`（09-17 遗留、躺在运行时 utils 目录里，功能已被 `prototype/_map_extract_weapp.cjs` 取代）。
- **packageArt 立绘冗余清理（净省 174KB）——我第一版判据不完整，已自我纠偏**：
  - 初判：`data.js` 的 `artUrl(k)` 只用 CHARS key 拼路径 → 我据此认定"stem 不是 CHARS key 的立绘文件永远解析不到"，找出 **15 组"中文名副本 == 拼音正本"字节完全相同**的冗余并删除（122 → 107 张，省 199.3KB）。
  - **纠偏**：立绘路径有**两个来源**，我只查了第一个。第二个是 **`weapp/utils/sect.js` 的 `SECT_ART_SET` + `art:'名字'` 字面量**、`pages/wushipai` 的 `w:'名字'` 字面量——它们用**中文名/别名**，与 CHARS key 集**不重合**（`顾粲` 是 CHARS key，但 sect.js 引用的是 `顾璨`；`miyu` 是 key，`米裕` 是别名）。其中 **`顾璨.jpg`、`米裕.jpg` 被硬引用，删了就成悬空引用**（sect.wxml 的 `image` 没写 binderror → 破损图）。
  - **已恢复** `顾璨.jpg` + `米裕.jpg`，并按既有规格重做（源 360×540 → 200×300，q74/q76，12.4/12.5KB，与其余 107 张一致）。目录 **109 张 / 1456.5KB，全部 200×300**。
  - **真正可删的是 13 个**：`佛祖 周密 宁姚 文圣 曹慈 白也 白泽 礼圣 道祖 阿良 陈平安 陈清都 齐静春`。`prototype/assets/portraits` 源目录**未动**。
  - **新增常驻门禁 `prototype/_port_ref_guard.cjs`**：同时校验两个来源（CHARS key via artUrl + `SECT_ART_SET`/`art:`/`w:` 字面量），任何"声明有立绘但文件不在"即失败（退出码 1）；另报"CHARS 角色无立绘"与"疑似死重"清单。报告 `_port_ref_report.txt`。**教训：判"死文件"必须枚举所有引用来源，只查一个必然误删。**
- **既有缺口（非本次引入，供决策）**：127 个 CHARS key 中 **30 个无立绘**；其中 `sect.js` 的 5 个弟子 `art:` 字面量指向不存在的图（`赵树下 宁吉 邓剑枰 袁黄 韦文龙`，因不在 `SECT_ART_SET` 内故走占位符、不是破损图）。另 10 个孤立立绘文件（`仙尉 初升 寇名 托月山大祖 新妆 朱敛真容 柔荑 萧愻 赵天籁 路人`）**CHARS 里没有对应 key**，其中两处像 key 写错：**`萧愻.jpg` vs CHARS key `萧𢙏`**（愻/𢙏 是两个不同字，疑似 key 打错导致该角色拿不到立绘）、**`托月山大祖.jpg` vs key `托月山`**（文件多带了"大祖"二字）。修需按"四表 key 一致"纪律统一，未擅自改。
- **立绘压缩评估（未落盘，我主动放弃）**：源 360×540 统一；`codex` 详情位是 180×270rpx，DPR3 下需 ~270×405 物理像素 → **200×300 的存储尺寸合理，不能再缩**。只调质量到 ≤12KB 时，**约 57 张要掉到 q62–q70、11 张 q58、6 张 q55**（插画细节密集，再压肉眼可辨）。判断：**不值得用画质换这 200KB**，已放弃；`packageArt` 84% → 若将来补 30 个缺立绘的角色（127 key 中仅 97 有图）**会再次逼近上限**，届时优先考虑"按显示尺寸存"或降低详情位尺寸，而不是硬压质量。
- **新增/更新的资产**：`prototype/_wx_size_guard.cjs`（体积门禁：主包/单包/**预下载合计**/总计四条红线）、`prototype/_port_ref_guard.cjs`（立绘引用完整性门禁：两个来源全查，悬空即失败）、`prototype/_map_verify.cjs`（地图回归 91 项）、`prototype/_map_extract_weapp.cjs`（矢量抽取）、`packageMap/*`。skill `jianlai-portrait-sync` 已修正（小程序落点从主包改到 `packageArt/assets/portraits`、**判定死文件必须同时查 `SECT_ART_SET` 与字面量**、加 preloadRule 2MB 红线与门禁、修正 Python 路径为 3.13.12）。

## 第三方方案评审（2026-09-18，用户拿另一模型的《项目整体评估与改进方案》问"有道理吗"）
- 结论：**七成可用、产品判断力不错，但有 4 处事实硬伤会让它把优先级排错**。产出 `剑来-方案评审与独立分析.md`（项目根，与既有 7 份报告同级）。
- **它错的 / 过时的**：① `_wx_probe`/`_pc_combo` 两门禁**全库不存在**（实际常驻是 `_sc_keys/_sc_smoke/_sc_battle/_narr_smoke/_pf_smoke/_lq_smoke/_map_verify/_wx_size_guard/_port_ref_guard`）；② "120 角色" → 卡牌池实为 **127**（game.js CHARS 与 briefs 侧一致，2026-09-18 实测；此前"108"系过时）；③ "PC 处决 0.2s" → PC 实为 **1.15s**（`index.html:667 #execfx.go`，白闪 0.34s），0.2s 只在未验证的小程序端；④ "packageArt 余 117KB" 已过时（现余 295KB）、"18 条音效"已过时（现 24 条）；⑤ **"模块孤岛、唯一闭环单向"高估**——实际 ≥3 条链；⑥ 把"加 `wx.vibrateShort`"当待办 → **`vb()` 已在 5 个模块实现**（profile/shenci/sect/baofuzhai/game），应降级为"补齐覆盖"。
- **它对的**：小程序端 SFX 全是空函数（`engine.js:27`）；DIFF 三档只调 `allyDraw/foeDraw/bossMul` 不改 AI；本命飞剑全角色共用唯一 `sub:'feijian'`；无 tutorial；不做阵营调平；"每条改动必须回答制造了什么新决策"。
- **真实的跨模块链（它漏了，证据）**：① 对战↔包袱斋**双向**：出 `game.js:1225 bfAddCoin(win?BF_COIN_WIN:BF_COIN_LOSE)`、入 `baofuzhai.js:306 bfApplyLoadout`（`game.js:1250` 调用）；② 落魄山→对战：写 `sect.js:2662 jianlai_sect_allies`、读 `game.js:1189 loadSectAllies()`；③ 包袱斋→行迹录：`profile.js:312` 读 `jianlai_baofu.coin`。→ **真问题不是"没连接"，是"4 套资源不通兑"**（雪花钱/香火功德/金精铜钱/声望）。
- **我的独立补充（它没看到的）**：① **双端参数漂移**——PC 与 weapp 是两套源码拷贝，`BF_COIN_WIN`/`TIER_LIMIT`/`REALM_LIMIT`/`JP_THRESHOLD` 靠人工同步、不一致**不报错**，冒烟查不出，建议加"双端常量一致性门禁"；② **存档迁移唯一不可逆**——现状 8 个独立 key（`jianlai_record/diff/sect_allies/baofu/profile/shenci/asklake` + `ljtq_v1`）只有一个零散 `BF_VER`，做"一本账"前必须先有 `SCHEMA_VERSION`+迁移骨架，否则打坏老存档；③ **"讲道理"不该做成"暗牌比点数"**——项目已有完整 judge 链（`game.js:1456 JP_THRESHOLD=6` + 【问心】【化外】【通晓】【至高】四技能挂在上面），再叠一套 = 同概念两套规则；原著"讲道理"是立规矩/争道统非比大小。替代方案=**把 judge 从被动概率改成主动投入**（判定时可选弃牌提升判定），复用现成 UI，且让所有 `judge+v` 角色立刻从数值变决策；④ **命名坑**：原著「天时/地利/人和」是**十四境合道三途径**（`game.js:236` 明载，礼圣走特殊合道不在此列），别拿它当"局内环境牌"的名字 → 建议改「**天象**」。
- **我给的优先级重排**：收官门禁（真机复验+双端常量门禁）→ 触觉/演出**补齐**（非新建）→ **天象 + 飞剑 subtype** → **108 全员技能触发复核**（它排 P2，我提前：108 角色共用一飞剑/一判定/一副牌库，差异只在技能文本，技能不触发则角色=立绘）→ 存档骨架 + **只读账目总览** → 讲道理 → 引导局。
- **小程序不做音效的理由（我强化它的结论）**：PC 24 条音效是 WebAudio 实时合成（零资源），小程序等价必须用真音频文件 → 会直接顶爆 86% 的 packageArt。正解是触觉 + 视觉补偿。

## 双端常量一致性门禁（落实上一轮建议①，已建 + 首跑抓到真漂移）
- 新增 `prototype/_dual_const_guard.cjs`：比对 PC 源（`game.js`→`weapp/utils/data.js` 自动生成链、`sect.js`/`shenci.js`/`baofuzhai.js` 手工副本）与小程序端的同名常量。归一化（去注释/去引号/去空白）后做字符串比对；CHARS/SKILLS 额外做"键排序无关"深度比对（eval 字面量→规范化为排序后 JSON 再比），既能抓真漂移又免疫键顺序差异。覆盖 4 组共 39 项常量（其中 `ASKLAKE_QUESTIONS` 两端均无、跳过），违反退出码 1，报告 `_dual_const_report.txt`。
- **首跑即抓到真漂移**：`weapp/utils/data.js` 郭竹酒 txt 残留旧值「饮者（酒）」而 `game.js`（权威源）已是「问剑（酒）」——data.js 未随 game.js 重跑 `gen_data.js` 所致（典型"改了 PC 忘了重生成"）。已重跑 `gen_data.js` 重新同步，`data.js` 格式也由 JSON 双引号归一为与 game.js 一致的单引号；二次运行 39 项全绿、报告归零。
- 已登记进 MEMORY.md §2 门禁列表 + §7 双端同步，并把旧记"卡牌池 108"纠正为实测 127（game.js CHARS 与 data.js 一致；briefs 侧亦 127）。
- **纪律**：改 `game.js` 任何常量（尤其 CHARS/SKILLS）后**必须重跑 `gen_data.js` 重生成 data.js**，否则小程序静默发旧数据；这道门禁是发布前兜底，与 size/port_ref/map_verify/selftest 并列必跑。
- 临时校验脚本 `_chk_game.cjs`/`_chk_game.txt`/`_chk_data.cjs`/`_chk_data.txt` 已清（仅本次调试用，非资产）。

## 一键回归入口 regress.cjs（串联 12 道门禁，2026-09-18 新增）
- 新增 `prototype/regress.cjs`：统一编排全部 12 项常驻门禁（`_sc_keys/_sc_smoke/_sc_battle/_narr_smoke/_pf_smoke/_pf_int/_lq_smoke/_map_verify/_wx_size_guard/_port_ref_guard/_dual_const_guard` + `weapp/_selftest.cjs`）。子进程复用 `process.execPath`（受管 node 下全走受管 node）；汇总 PASS/FAIL、写 `_regress_report.txt`、任一 FAIL 则退出码 1；`--list` 仅列清单。**只收录磁盘真实存在的脚本**，避开记忆里幻觉的 `_wx_probe`/`_pc_combo`。
- **首跑即抓到一个预存在的失效门禁**：`_narr_smoke.cjs` 第 14 行 `require('weapp/utils/shenci.js')` 报 `Cannot find module`——shenci 模块已随分包重构搬到 `weapp/packageShui/utils/shenci.js`（sect.js 仍在 `weapp/utils/`）。已更正该脚本第 14 行 require 与第 147 行 `evalBlock('...SC_ENDINGS')` 两处路径。修复后全量回归 **12/12 PASS、EXIT=0**（耗时约 84s，卡牌对战 `_sc_battle` 占 ~71s）。
- 已登记进 MEMORY.md §2（统一入口 + narr_smoke 路径坑）。发布前必跑 `node prototype/regress.cjs`。

## 触觉补齐：小程序对战引擎（2026-09-18，优化路线第 2 档"触觉/演出补齐"）
- 现状核实：vb() 只存在于 4 个 weapp 模块（落魄山/山水祠/行迹录/包袱斋），**小程序对战引擎 `engine.js` 完全没有触觉**——其 `SFX` 是无副作用空函数对象，对战全程调 `SFX.turn/die/slash/heal/...` 但小程序既无音效也无震动。这是"小程序走触觉+视觉补偿"策略明确要补的缺口。
- 改法（低风险、一次改 SFX 对象、不碰 30+ 调用点）：在 `engine.js` 加 `vb(t)`（与 sect/shenci/baofuzhai/profile 同范式，`typeof wx` 缺失自动降级为无副作用），并让 `SFX` 各方法挂 light/medium/heavy 三档触觉（点击/抽牌=light，回合/攻击/破阵/剑雨=medium，阵亡/胜/负/处决=heavy）。
- 验证：`node -e require('./weapp/utils/engine.js')` 在 Node 下加载 OK、导出完整（init/startGame/onCardClick/...），`wx` 缺失时不抛错。临时校验脚本 `_chk_engine.*` 已清。此改动仅 weapp 侧、未被 regress 的 `_sc_battle`(测 game.js PC)/`weapp/_selftest`(不引 engine) 覆盖，故单独做了加载验证。
- 进度：触觉补齐已覆盖小程序 5 处（4 叙事经营 + 对战引擎）；笼中雀/悟世牌暂无反馈，待后续按需补。下一步优化档：天象 + 飞剑 subtype → 资源账目总览 → 讲道理 → 引导局。


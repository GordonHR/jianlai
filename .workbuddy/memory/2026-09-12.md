# 2026-09-12 工作日志

## 戏里戏外（tales.js）扩容
- 在 `prototype/tales.js`（PC 全局 `const TALES`）与 `prototype/weapp/utils/tales.js`（同数组 + `module.exports`）两处各追加 8 则原著小记，双端 1:1 同步：
  peiqian 裴钱 / cuidongshan 崔东山 / caoci 曹慈 / baiye 白也 / weibai 魏檗 / zhengjuzhong 郑居中 / zhoumi 周密 / chenpingan_benming 陈平安·笼中雀与井中月。
- 内容守原著纪律：裴钱/崔东山为落魄山弟子、青萍剑宗首任宗主崔东山、魏檗神道履历（北岳→沉江→棋墩山→披云山·夜游→住山大使）、陈平安本命飞剑笼中雀+井中月（自育）/初一·十五（赠剑胚）均取自 MEMORY 已核实设定；周密线为诠释性（src 标「旧天庭·新天庭」），quote 一律留空不杜撰原句。
- 机制：两端 `renderTales()` / `codex.wxml` 均直接遍历 `TALES` 数组，纯数据追加即自动入列表与菜单计数，无需改渲染。
- 验证：`node --check` 双端 0 错；`_sc_smoke.cjs` 23 通过/0 失败（含 PASS 13/14/15 戏里戏外）；原 TALES 数量断言 `>=9` 满足（现 27）。
- 无图片：戏里戏外沿用纯文字画卷（轴线金线+朱印+宣纸底纹），与用户此前「符箓图片仅 PC、戏里戏外不配图」决策一致。

## PC 对战布局 · 三国杀式优化
- 范围：仅 PC 端 `prototype/game.js` + `prototype/index.html`，未动微信端（用户只说 PC）。
- 改动 1（核心信号）：`hpBar()` 由连续血条改为**三国杀式血量点**——`maxHp` 个圆点，实心=现有、空心=已失，附 `x/y` 计数与悬浮 `realm` 提示；敌人卡与武将卡共用，武将卡点略大。
- 改动 2（围坐感）：战场加「牌桌」绒底——`.bf-field::before` 椭圆桌影 + `.bf-stage::before` 中央出牌区圆桌面（低透明径向渐变+内阴影+金线描边），敌我在桌两侧落座。
- 已有且契合的 三国杀要素：四角阵营身份角标（`fac-over`）、底部扇形手牌、当前行动者高亮（`.cur`）、金底「X号位」座次牌——均保留未动。
- 验证：`game.js` `--check` 0 错；`_sc_smoke.cjs` 23 通过/0 失败。
- 未做（待用户拍板）：完整圆形环坐（所有角色沿椭圆周长分布）属大重构，未动；如需可另议。

## 手机端符箓 · SVG 图样（与 PC JPG 同体例）
- 用户原话：「手机端符箓可以做点svg，你能做出一样的效果吗」——此前决策「符箓图片仅 PC」在此反转：手机端改上 SVG 云篆图样。
- 生成：写一次性 `_gen_fulu_svg.py`（纯字符串拼 SVG，未留），按 PC JPG 体例输出 17 张：黄纸底 `#e7d6a4` + 朱砂/墨云篆 + 符头（三角云篆头+中竖+内横）+ 符胆（每符独立母题：缩地嵌套折方/挑灯火苗/宝塔层叠/破障中竖破横/静心同心水/祛秽横扫/山河长竖如剑/求雨云头降雨/烧煞辐射/霆司雷篆锯齿/横流横波/撮壤叠土/路引开门兽引/过桥拱桥/兵符三组锐笔/五雷五辐射/镇剑巨剑沸海）+ 左右云篆小字列 + 符脚 + 朱印（红方块+纸色挖空似字）。手绘抖动烘焙进几何（二次贝塞尔+种子扰动），**不依赖 SVG filter**，确保小程序原生渲染一致。
- 包体铁律触发：实测主包 2,525,068 B（已超 2MB 约 428KB，无分包）→ 直接加 17 张 SVG（~245KB）会冲到 2.77MB。**按「加图必须先分包」铁律，把 `pages/fulu` 整页 + 17 SVG 迁入新建分包 `packageFulu`**（python shutil 移动：page 4 件套→`packageFulu/pages/fulu/`、SVG→`packageFulu/assets/fulu/`；app.json 加 `subPackages`、主包 pages 移除 fulu；`title.js` goFulu→`/packageFulu/pages/fulu/fulu`）。
- 接线：wxml 列表卡加 `<image class="fl-thumb" src="/packageFulu/assets/fulu/{{item.id}}.svg">`，详情层加 `.fl-dt-img` 大图；wxss 加 `.fl-thumb(150rpx)/.fl-dt-img(320x440rpx)`；标题文案「PC 端可见」→「双端可见」。
- 验证（脚本实测）：app.json 合法；主包降至 **2,266,942 B**（迁出符箓反而减 ~258KB，仍超 ~170KB 为历史遗留，与本次无关）；分包 `packageFulu`=258,713 B（<<2MB）；17 SVG 全部 XML 合法；无旧路径悬挂引用。临时脚本已删。
- 遗留待办（独立）：主包仍超 ~170KB，根因 portraits/shenci/sect 图资挤在主包；需把重图资也迁分包才能正式上传——本次未动，已写入 MEMORY §0。

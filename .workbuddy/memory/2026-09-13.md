# 2026-09-13 工作日志

- **手机端符箓图样回退为纯文字（用户判"太丑了"）**：上一轮生成的 17 张符箓 SVG（程序随机笔划拼的"伪云篆"，符胆像儿童涂鸦）被用户否决。经确认选「手机端不要图」。
  - 删除 `weapp/packageFulu/assets/fulu/` 下 17 张 SVG 及空目录。
  - `packageFulu/pages/fulu/fulu.wxml`：去掉列表缩略图 + 详情大图两处 `<image>` 引用。
  - `fulu.wxss`：删 `.fl-thumb` / `.fl-dt-img` 图片样式 + 头部注释改回"纯文字"。
  - `fulu.js` 头部注释、`title.wxml` 入口文案「双端可见」→「仅 PC 端可见」。
  - 分包 `packageFulu` 现仅 ~13KB（页面四件套），全项目无 `assets/fulu` 悬挂引用。
- **教训（重要）**：程序几何生成的"云篆符箓"远不如 AI 位图自然，且随机笔划容易像乱线/涂鸦。**手机端若真要符箓图样，应复用 PC 同款 `assets/fulu/<id>.jpg`（Pillow 压小到 ~300px、单张≈25KB）放进 `packageFulu` 分包，而非矢量重画**。本次用户选纯文字，故未走此路。
- 未动：PC 端 17 张 JPG 符箓（用户此前认可）、主包超标遗留（portraits/sect/shenci 图资挤主包，约超 170KB）。

## 交互优化（PC 对战，引擎安全，仅改 game.js / index.html）
- **键盘操作补全**：原仅 ESC。现加 ① `Enter`=打出已选牌（复用 `playSelected`）；② 数字 `1-9`=按手牌序选中/再按打出（复用 `onCardClick` 两步出牌）；选目标期间键盘只响应 Esc。改 `game.js` DOMContentLoaded 的 keydown 监听。
- **选目标焦点强化**：`state.await` 时给 `.battlefield` 加 `awaiting` 类，CSS 把 `.player/.hero:not(.clickable)` 压暗（opacity .4 + 灰阶），可点目标脉冲金边更突出（仿三国杀「指定」态）。
- **自点英雄卡补「▼ 可选」标**：`heroBody` 在 `await && allowSelf && alive` 时加 `.tag.tgt`（此前敌方卡有、自己的卡缺，不一致）。
- **确认条加键盘提示**：`confirmBarHtml` 出牌按钮标 `↵`、加 `cb-hint`（回车出牌·再点卡牌亦可·Esc 取消）；CSS 加 `.cb-hint`/`.cb-key`。
- **战报攻击行上红色**：`playCard` 攻击线 `log(...,'')`→`'hit'`（`.lg-hit` 红字），与 heal 绿字、sys 灰斜体一致。
- 验证：`game.js --check` 0 错；`_sc_smoke.cjs` 23/0。
- 未动：手机端（此前纯文字决策）、触发主包迁移。

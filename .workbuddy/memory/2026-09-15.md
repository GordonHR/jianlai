# 2026-09-15

- 剑来立绘小红书涨粉方案交付：在 `剑来小红书/` 产出 5 文件（01策略脑图、02人物志排版模板、03-30天选题日历、04样例-陈平安、05样例-宁姚）。高清立绘源 = `prototype/art/*.png`（非小程序 weapp 那份压缩 jpg）；场景大图 = `prototype/bg/`（合集封面海报/群像长卷等）。定位：纯做号涨粉，人物志系列为主引擎，配比 70/20/10。

- 骊珠洞天地图交付：`C:\Users\Administrator\Desktop\jianlai\骊珠洞天地图.html`（自包含 HTML+SVG，古风墨线，按东/西/中/南/北分区标注，附完整地点考据图例）。严格依《剑来》原著 + 落魄山 WIKI 地理堪舆辑录，未臆造地点/称谓；含龙形堪舆、铁锁井/老剑条镇锁龙井、龙窑建于真龙尸骸脊柱窍穴、东门外无龙窑等设定。

- 骊珠洞天地图升级为「交互古舆图」：`骊珠洞天古地图.html` 已重画为手绘墨线风并被用户采纳。技法：① SVG `feTurbulence+feDisplacementMap` 滤镜（id=rough，scale≈3.4）套在"地景层"让所有墨线自然颤抖（取代生硬直线）；② 西边大山改水墨层叠（远山/主山渐变 + 沿坡皴法短笔触 + 苔点）；③ 龙须溪贝塞尔婉转双岸 + 流水纹；④ 小镇围垣改手绘不规则双线土墙（取代虚线方框）；⑤ 图例符号（房/树/龙窑/城门/桥/牌坊/井/墓/祠）全换带笔触有机造型；⑥ 纸面斑渍做旧。交互层（46 热区 + 45 条 DESC 原著详解）与考据附录原样保留。注：Write 整文件覆盖前须先 Read 当前文件（工具前置约束），否则报 "File has not been read yet"。

- 骊珠洞天古地图第三轮打磨（用户反馈"图上字太长/要侧边弹框/图还需优化"）：① **地图标签全部压成 2–4 字短名**（泥瓶巷/陈平安宅/铁锁井/廊桥/龙窑…），长副文本与考据说明移入"点击后右侧抽屉"；② **详情改为右侧 sticky 侧栏抽屉**（flex 两栏布局，窄屏 <920px 自动堆叠），替代原底部面板，更多介绍放右侧；③ **视觉再升级**：新增 `mtnMist` 远山雾层 + `haze` 高斯晕染（山脚/水边）、西边大山皴法加密、龙须溪水色更通透、加天空飞鸟三笔 + 水面远帆一笔、纸张 grain 提至 0.7、rough scale 降至 2.8 更稳。校验：46 热区×45 详解零缺口，残留长文本仅存附录/侧栏。

- **BUG 修复（骊珠洞天点击失效）**：用户反馈"点击没有效果"。根因 = `DESC` 内联 JS 对象中 `songjixin` 那条 text 字符串混入了英文双引号 `同巷的"小少爷"。`，导致外层英文双引号 JS 字符串提前截断、整段 `<script>` 解析崩溃，所有 `addEventListener` 未绑定（CSS hover 高亮仍在，故用户能看到悬停变红但点不动）。修法：① 把内联引号统一改中文「」；② 给 `.poi`/`.hit` 加 `pointer-events:all` 兜底透明命中区。校验用 `node --check` 提取的 script，输出 `JS_SYNTAX_OK`。**可迁移铁律**：HTML/SVG 里内联 JS 数据对象（DESC/数据表）的所有用户可见引号必须用中文「」/『』，绝不可嵌套英文双引号；写完务必 `node --check` 一遍，否则一个字符就让整页交互死掉。

- **骊珠洞天古地图第四轮：全屏单图 + 触发式抽屉**（用户："只留主图并撑满；考据文字不要铺开、要点击触发；顶部说明不要"）。改动：① 删除顶部 h1/sub/hint 横幅，主屏只剩地图；② `.mapstage{position:fixed;inset:0}` + `svg{width:100%;height:100%}` + `preserveAspectRatio="xMidYMid meet"`，地图等比撑满视口居中；③ 原页面底部 7 节考据附录整体移入右侧抽屉的 `#panelLegend`（默认 `hidden`），与地点详解 `#panelPoi` 通过 hidden 切换；④ 新增 2 个 `.trigger` 触发点——右上朱印与左下「图例」框（图例框内加小字"▸ 点击此处·右上朱印，展开全图考据"提升可发现性），点击调 `showLegend()` 打开抽屉显示全图考据。校验：POI 45×DESC 45 零缺口、trigger 2、`JS_SYNTAX_OK`。

- **骊珠洞天古地图第五轮：街巷与交通明显化**（用户："巷子和交通体现的还不够明显"）。改动：① 原"隐约街巷"（opacity 0.14 淡虚线）替换为 `class="roads"` 手绘路网层——东西向主街（福禄街—市井—东门）与南北向主街用 `#e6d6b0` 宽亮带(12~13px)+`#b89b5e` 描边表现"石板街"体量；泥瓶巷/骑龙巷/扁担巷/杏花巷/二郎巷等用 `#9a7b2e` 2.4~3px 窄墨线串起院落；② 四条对外交通用 `#caa86a`/`#b9925a` 土黄粗线：东门云梯出镇路(东北出框)、龙须溪南岸路(溪畔路)、镇南黄泥小路(→小庙/父母坟)、西边入山道；③ 骑龙巷加 8 条台阶短横纹（呼应原著"一百二十二级台阶"）、东西主街加 6 组石板缝；④ 新增淡墨路名标注 5 处（福禄街/云梯路·出镇/入山道/溪畔路/南下黄泥路）。图层顺序：围垣 → roads → 图例符号(house 等) → 路名/POI 文字，保证街道在"地面"、建筑文字在上。校验：POI 45×DESC 45 零缺口、`<g>` 77/77 配对、`JS_SYNTAX_OK`。

- **骊珠洞天古地图第六轮：主街/巷子对比拉开 + 三龙堪舆显形**（用户："全部，但是要按原著"）。先联网核「三龙」原著设定（多源互证）：龙须溪+主街=二龙须(一隐一显)；福禄街=龙颈(街上每座府邸即符箓，镇龙颈气府防"抬头")；桃叶巷=龙脊(每株桃树根须入地=困龙钉，禁锢龙脊)。改动：① **主街/福禄街提亮加宽**（`#f1e4c4` 宽 15~16 + `#caa86a` 描边），**巷子收窄**（`#7a5a32` 2.3~2.6，明显 subordinate）；福禄街加"符箓纹"短竖线呼应原著；主街加竖向石板缝；② **三龙堪舆朱色虚线**显形：`class="kan"` 组沿福禄街(龙颈)/桃叶巷(龙脊)/主街(显龙须)/龙须溪(隐龙须)画 `#9c3b2e` 虚线；桃叶巷加 5 枚深色"困龙钉"小钉点；③ 地图加 3 个可点朱标「龙颈/龙脊/龙须」(`class="trigger" data-kan`)，点开右侧抽屉并 `scrollIntoView` 跳到新增的「八、龙形堪舆·困龙局」考据段（含来源）。校验：POI 45×DESC 45 零缺口、data-kan 触发 3、`<g>` 84/84、`JS_SYNTAX_OK`。

- **骊珠洞天古地图第七轮：真地图缩放/平移交互**（用户："可以做到像地图一样放大吗，我说真的地图"）。改动：① 给 `<svg>` 加 `id="map"`，把全部地图内容（纸面→地景→POI→朱印/图例触发）包进 `<g id="viewport">`，平移缩放只作用于该组；② **滚轮缩放**：`wheel` 监听 + `zoomAt(cx,cy,factor)` 以光标位置为锚点（用 `svg.getScreenCTM().inverse()` 把屏幕坐标换算到 viewBox 坐标，保持光标下点不动）；范围 0.6~9×；③ **拖拽平移**：`mousedown/mousemove/mouseup`（window 级），`dx/ctm.a` 把屏幕像素换算回 viewBox 单位；④ **触屏双指捏合** `touchstart/move/end` + `d2/mid2` 算两指距与中点；⑤ 右下角 3 个最小控件 `＋ / － / 复位%`（`.zoom` 绝对定位，非顶部横幅，符合"顶部说明不要"）；⑥ **拖拽 vs 点击区分**：全局 `moved` 标志（位移>3px 即 true），POI/trigger 的 click 里 `if(moved){moved=false;return;}` 防止拖拽误弹详解。校验：POI 45×DESC 45 零缺口、`</g>` 85/85 闭合、控件齐全、缩放/捏合/滚轮/moved 守卫全在、`JS_SYNTAX_OK`。

# 2026-09-16

- **骊珠洞天古地图（第十一轮续·第十二轮）：铁锁井重叠 + 廊桥落位 + 原著续优化**（用户："铁索井跟别的重叠了，廊桥没有定位到河上，其他的根据原著继续优化"）。修复：① **铁锁井重叠**——原井符(585,676)+红圈(600,688)正压扁担巷泛用房符(580,680)，且缺可见巷道。改为加「杏花巷」巷道(path 自东横街 612,664 南下至 614,694)、井符移到(602,694)、红圈(608,698)、标签右移到(640,700)，并把冲突房符(580,680)→(558,690)、删(580,664)房符，铁锁井现清晰落在杏花巷底端、不再压房。② **廊桥未落河上**——原廊桥符号在 translate(596,826)(y826+)，已落到溪南岸路、整体在河面以南约25px。改为 translate(594,756) scale(1.1) 跨在龙须溪水面(河在 x≈594 处 y≈774–796，桥 756–807 正好压河)；主街末端改接到桥北(614,780)、南下黄泥路改从桥南(614,800)起、删冗余「廊桥接驳」短线；困龙局朱标主街末端同步改(614,780)。③ **锁龙井标注**——桥底深潭旁加青色「锁龙井」小字(556,804)，与杏花巷红色「铁锁井」明确区分(呼应前期用户强调的二者不可混淆)。④ **龙须溪源头**——西边大山脚下河段西端加泉眼符号+「龙须溪·源」小字(232,706)，依原著"源出西边大山·本名浯溪"。校验：`<g>`97/97、`<svg>`/`<defs>`/`<script>`单开单闭、use 47 零缺失、POI53=DESC53、`node --check` OK。交付 `骊珠洞天古地图.html`。

- **可迁移铁律（骊珠洞天地图系列）**：同文件多次 Edit 务必**顺序单发**（并行偶发只生效部分）；内联 JS 数据对象文本只能用中文「」/『』，绝不可嵌套 ASCII 双引号（否则整段 `<script>` 崩）；校验脚本用 `node --check` 抽 `<script>` 跑最稳，POI×DESC 一致性 + `<g>` 配平 + `<use>` def 覆盖三轮校验必跑。

- **浩然天下堪舆图（第十五～十七轮续改）**：用户连续纠偏——① 九洲"离太远"且"宝瓶洲应最小、其余加大"：重排 WORLD 数据，**质心反算 wx/wy**（land path 局部质心 ×ws 偏移，使洲名真正落在洲块上）；**中土最大(ws0.40)、宝瓶洲最小(ws0.18)、其余七洲 ws0.34/0.32 加大**，并紧贴中土减小海隙、加跨洲航道虚线连成群岛。② **宝瓶洲必须是花瓶轮廓**（原著："一洲山河宝瓶洲，恰似那一只人间某处书案上的清供花瓶"）：把 `land-bao` defs 改为花瓶 path（瓶口在北=骊珠洞天、瓶底在南=老龙城），并**删 ZHOU.bao.detail.territory**（原先"色块+淡花瓶虚线"叠影是"奇怪"根源）。③ **剑气长城位置纠偏**：用户以为"不该在东边"，但原著铁律——剑气长城位于**浩然天下与蛮荒天下的交界处**、蛮荒天下在浩然**东侧**，故剑气长城是浩然**东界**（宝瓶洲最东、正贴）。把长城从飘在最边缘(1540)贴到宝瓶洲东侧(1360)，显式补"东海→剑气长城→蛮荒天下(长城以东·妖族祖地)"东向层次。校验均 `node --check` OK、`g` 配平。

- **剑来原著铁律补（地图用）**：九洲面积——中土神州最大（约≈其余八洲之和），**宝瓶洲九洲最小**（多源互证）；剑气长城=浩然天下**东界**、隔蛮荒天下（蛮荒在其东）；倒悬山在婆娑洲东南、是浩然→剑气长城唯一常规中转（余斗所炼山字印，入境签山盟压半境）。

- **骊珠洞天古地图（第十三轮）：西边大山峰名去"列表化"**（用户："山的标注还能优化吗？现在山名放一起像是列表一样，不像地图"）。根因：14 座峰名全堆在右侧 x≈252 一条竖列(y 200→512 每 24px 一行)，像名单。改法（古舆图峰标惯例）：把每座峰名**落回对应山头/山脊**，用细墨引线 + 峰顶小点(dot r1.8)标注、左右交错散布——披云山(188,150)最高峰置顶·远幕峰(150,170)/神秀山(144,205)向左·彩云峰(200,160)/横槊峰(164,250)/梧桐山(210,212)/牛角山(224,250)/黄湖山(250,278)/宝箓山(244,300)/灯芯台(230,300)向右·挑灯山(92,330)/地真山(100,360)向左下·仙草山(236,470)/真珠山(252,640)落山脚 Slope；区域名「西边大山」从竖列区移到山脚左下(54,500)+(54,518)小字"六十二峰·封禁之山"作范围标注。校验：`<g>`97/97、POI53=DESC53、use 47 零缺失、`node --check` OK。交互不变。

- **新建《浩然天下堪舆图》分层钻取系统（第十四轮/新文件）**（用户："生成浩然天下整体地图，各洲分别地图点击打开，宝瓶洲点击骊珠洞天又能打开你刚做的地图，串起来"）。新建单文件 `浩然天下地图.html`，三视图架构：**world 视图**（九洲总图，按原著方位：中土居中·宝瓶东·北俱芦东北·皑皑正北·桐叶东南·婆娑正南·扶摇西南·流霞西北·金甲正西，加四海+倒悬山）每洲可点下钻；**zhou 视图**（数据驱动，ZHOU[key]={name,sub,desc,places[]}，每洲标注原著明确核心地标，仅列明确地名不臆造）可缩放/拖拽/点击详解+返回按钮；**lizhu 视图**（宝瓶洲 places 中「骊珠洞天」标红点，点击→iframe 全屏加载 `骊珠洞天古地图.html`，带关闭按钮，复用现成文件全部交互）。九洲方位与地标均先联网核查（落魄山WIKI/搜狗/今日头条多源互证），不编造。校验：`<g>`18/18、SVG/style/script/aside 单开单闭、10 洲 world↔ZHOU key 零缺口、`node --check` OK、引用骊珠洞天古地图存在。注：本文件无 `<defs>`（不用 symbol，合法）。**坑复现**：同消息发两个 Edit 修正校验脚本，第一个(defs 判定 ok=(o==c))仍偶发未落地，单发才生效——再次印证"同文件多 Edit 顺序单发"铁律。

- **浩然天下地图（第十五轮·重写）：真古舆图风**（用户："太简陋了，像随意几何图形，要真像地图、贴合原著比如宝瓶洲形状、要有路网、要有点击后介绍"）。第一版被批"几何图形"。重写要点：① 海洋底纹(`url(#sea)` 波纹 pattern + radial 渐变) + 四海/剑气长城(东界红虚线)/倒悬山标注；② **九洲有机不规则海岸线**：各洲 `<path id="land-X">` 手绘 bezier 轮廓(不再是直线多边形)，`rough` 滤镜让海岸自然颤抖；③ 符号体系 `#mtn`(皴法山)/`#mtnS`/`#city`(城)/`#capital`(都)/`#sect`(宗门)；④ **宝瓶洲按原著"清供花瓶"轮廓精绘**(剑来3156/1301章明写"一洲山河宝瓶洲，恰似那一只人间某处书案上的清供花瓶，在花瓶之内开出一大朵金色莲花")——瓶口=最北骊珠洞天、瓶底=最南老龙城；放大金**大骊势力范围**淡彩 + **齐渎**大河 + 云霞/正阳/真武山 + 风雪庙/神诰宗 + 书简湖 + 老龙城 + 青鸾/彩衣藩属 + **路网(roads 串联各要地)**；⑤ 其余八洲(中土/北俱芦/皑皑/婆娑/桐叶/扶摇/流霞/金甲)各加有机轮廓+山体+江河/路网+原著要地名标+点击详解；⑥ 点击洲下钻、点击骊珠洞天→iframe 进古地图、面包屑返回。校验：node OK、`<g>`16/16、9 洲 land 路径齐全、use 引用零缺失、iframe 目标存在。修复：`fill="var(--land)"` 改字面色值 `#ddd0ad`(呈现属性里 var() 解析不稳)。"land-detail 缺失"是校验脚本正则把嵌套 `detail:{` 误当洲 key 的误报，非真问题。
- **可迁移铁律（地图系列·补）**：SVG **呈现属性**(`fill=`/`stroke=` 等 attribute)里不要用 `var(--x)`（部分浏览器不解析），用字面色值；`use href` 引用的 symbol/path 必须在 `<defs>` 存在；九洲轮廓与要地必须用 bezier 手绘有机形，绝不用直线多边形（用户明确反感"几何图形"）；天下级地图必带海洋底纹+海岸线+山体+路网+点击详解四要素才像"真地图"。

- **浩然天下地图（本轮·桐叶洲叶形 + 青绿山水 + 画风切换）**：① **桐叶洲轮廓**改为叶片形（上阔下尖带叶柄）呼应"桐叶"之名；几何探针(16 段 bezier 采样+射线法)验证 9 POI+5 山全落陆内，bbox 243,332–671,521。② **全图转青绿山水**：石绿陆地(#5a9b6e)+石青山峰(#356f8c)+青绿海水(radial #dcebe7→#a9cbc6)+泥金/深绿勾边(#2e5538)，沿用 rough 滤镜作勾勒线。用户判断"蓝绿山水画更合适"——比原水墨更贴浩然天下仙侠语境；PC 端纪律允许渐变/光晕。③ **新增画风切换**：全色板改为 CSS 变量(--land/--ocean0/1/--wave/--mtn-f/s/in/--river/--land-stroke)，挂 `[data-style]` 于 `.mapstage`，三套——青绿山水(默认)/水墨淡彩/金碧；左下 `.stylebar` 分段控件实时切换(改 data-style 即生效，无需重绘)+ localStorage 记忆。④ **踩坑复现**：同消息并行发 6 个 Edit 仅 2 个落地（其余 10/57/59-61/62/64 仍旧值），再次印证"同文件多 Edit 必须顺序单发"；已逐一补完。校验：`node --check` OK、`<g>`17/17、3 按钮齐、land use 全存在。

- **浩然天下地图（本轮·删倒悬山航线 + 扩至六画风）**：① **删倒悬山航线标注**：用户认定 world 视图那条"浩然→倒悬山→剑气长城"虚线航路+说明文字+三角标记"着实没必要"。整块删去(lines 432-437 原 block)；倒悬山正式介绍仍保留在**南婆娑洲详情**的 daoxuan POI（点开南婆娑洲→倒悬山看完整设定），满足"点开到悬山有介绍就行"。② **画风扩至 6 套**：在用户认可的三套基础上，再加**浅绛山水**(赭石山·文人淡水彩)/**月夜图**(靛蓝夜海·月白岛屿·朱砂最跳)/**秋山**(暖赭金林·枫朱)，各为独立 `[data-style]` CSS 变量块 + `.stylebar` 按钮；`.stylebar` 加 `flex-wrap+max-width(100vw-32px)` 防窄屏溢出；默认仍青绿山水。校验：`node --check` OK、`<g>`16/16、航线坐标(1046,648)/标签(云海倒悬)确已删除、6 按钮齐、daoxuan POI 介绍仍在。

- **浩然天下地图（本轮·真换画风 + 桐叶洲按原著重画）**：① 用户指出"6 套其实只是换颜色，能不能真换画风，参考陈白熊写实堪舆图"。→ 加**两套结构性（非配色）画风**：**写实山川**(topo)——纯 SVG 滤镜 `#relief`(feTurbulence→feDiffuseLighting 地形晕渲)贴到陆地、`#seaTex`(turbulence 白云 screen 叠海)做云海水汽、皑皑/北俱芦 `class="snowCap"` 雪原覆层、topo 下 `display:none` 隐去符号化山符；**墨刻白描**(ink)——陆地改 `<pattern id="hatch">`(45°斜线阴刻纹样)填充。实现靠 CSS 覆盖呈现属性：`.mapstage[data-style="topo"] .landfill{filter:url(#relief)}` 覆盖其 `filter="url(#rough)"` 属性（CSS 优先于呈现属性，无需改属性）；海洋底图加 `class="seabg"`、山符加 `class="mtnG"` 作钩子；world 陆地 stroke 由硬编码 `#2e5538` 改 `var(--land-stroke)`。画风菜单 8 项(qinglv 默认在 :root，无独立块；其余 7 个有 `[data-style]` 块)。② **桐叶洲按原著重画**：WebSearch 多源证实原著明写"**桐叶洲状如桐叶**，与宝瓶洲相邻"——我上一版为圆坨(旧叶片改动确实丢盘)，改为**宽大阔卵形桐叶**：西侧叶基宽圆、东侧尖长叶尖、下带叶柄；几何探针(18 段 bezier 采样+射线法)验证 **9 要地 + 5 山全落叶内**，blade bbox 304,330–698,501。校验：JS OK、`<g>`16/16、3 滤镜+1 pattern、8 按钮齐、file 47KB。
  - **可迁移铁律（多画风）**：真正"换画风"≠换色——要么换**填充材质**（pattern/图片）、要么换**渲染滤镜**（feTurbulence+光照做晕渲）；CSS `filter`/`fill` 规则可覆盖 SVG 元素的同名**呈现属性**，是实现"同结构、多渲染"的零侵入开关。滤镜方案自包含但伤性能（大区域 turbulence 在缩放时会重算），慎用高 numOctaves。

- **浩然天下地图（本轮·撤销伪写实 + 切换改下拉）**：用户判"写实山川太不写实，还是继续维护地点路网吧，切换改成下拉不要铺开"。① **撤掉 topo/ink 两套伪写实**：删 `relief`/`seaTex` 滤镜 + `hatch` 纹样(defs)、删 `[data-style="topo"]`/`[data-style="ink"]` CSS 块及 `.snowCap`/`.mtnG`/`.seabg` 钩子；渲染里 `class="seabg"`(海洋矩形×2)、`class="mtnG"`(world 装饰山×2 + zhou 山符)、`snowCap` 雪原覆盖(if aiai/beiju)全部回退为纯色 style 渲染。现**仅 6 套传统设色画风**（青绿山水默认/水墨淡彩/金碧/浅绛山水/月夜图/秋山）。② **切换 UI 由铺开 8 按钮 → 下拉框**：`.stylebar` 内 `<button data-st>`×8 改 `<select id="styleSel">`(6 个 option)，CSS `.stylebar button*` 改 `.stylebar select*`；JS `querySelectorAll('#stylebar button')`+点击绑定 改 `getElementById('styleSel')`+`change` 事件；`setStyle` 加 `STYLE_KEYS` 白名单，陈旧 localStorage('topo'/'ink') 兜底回青绿山水。校验：`node --check` OK、`<g>`16/16、伪写实引用全 0、6 option+select+change 就位。③ **内容侧**：地点/路网/江河/山体渲染路径未动，继续维护地理数据。

- **浩然天下地图（本轮·倒悬山节点补回 + 画形象）**：用户纠偏——上轮删"旁边乱线"时把**倒悬山三角标记也连带删了**，导致 world 视图倒悬山节点整个消失（仅南婆娑洲 POI 介绍还在）。① **补回可点击倒悬山节点**：在 renderWorld 的 WORLD.forEach 之后、`scene.innerHTML` 之前，加 `<g class="cont daoxuan-node" data-drill="posuo" data-poi="daoxuan">`（位置 ~958,872，即原三角处，浩然东界·宝瓶洲以南·近剑气长城）。② **点击逻辑扩展**：`attachCont` 的 go() 改为读 `data-drill`+`data-poi`，有 poi 则 `drill(key)` 后 `show(poi,key)`——点倒悬山→直接下钻南婆娑洲并弹出 daoxuan POI 介绍（余斗山字印/唯一常规中转雄关/签山盟压半境）。③ **画形象**：三峰"山"形印(金 #b5892f + 深棕描边) + 朱砂圆印章内"山"字(#9c3b2e) + 上方云海倒悬曲线 + 标签"倒悬山/云海倒悬·唯一关口"。不画任何航线（用户只要删乱线、留节点）。校验：`node --check` OK、节点/点击/印章/POI 介绍全 PASS、旧航线坐标(1046,648)与航线说明确已删。

- **浩然天下地图（本轮·九洲宗门地点路网大补）**：用户"继续补充各州宗门、地点，路网"。① **扶摇洲修违例 POI**：原「纳兰烧苇」「刘蜕老祖」单人名立 POI 撤回，改为地名「烧苇剑庐」「刘蜕洞府」，人名写进详文；新增「扶摇残城」。② **金甲洲扩**：补 金甲王朝(王朝)/古战场/铁甲大营，路网 4→7。③ **南婆娑洲扩**：补 颍阴陈氏(世家，据 tales.js 原著位)，路网 3→6。④ **北俱芦洲扩**：补 骸骨滩/鬼蜮谷（披麻宗镇守绝地，原在详文里现已立 POI），路网 5→8。⑤ **中土神州扩**：补 文脉书院群/武庙，路网 6→10。⑥ **宝瓶洲补**：落魄山（陈平安开宗地，紧邻龙泉郡），路网加落魄山道/龙泉骊珠道。⑦ **全洲路网对象化**：原先仅宝瓶洲是 `{d,name,from,to,desc}`，其余八洲全是裸 path 字符串——全部升级为命名官道对象（67 条全带 name/from/to/desc）。⑧ 符号分类表 isSect/isCap 扩：剑庐/洞府/下宗/福地入宗门符，世家/军营入都城符。校验：JS OK、`<g>`16/16、九洲 ZHOU 齐、人名 POI 全 REMOVED、places=73 roads=67 named=67 strRoads=0。交付 `浩然天下地图.html`。

- **浩然天下地图（本轮·全优化遵原著）**：用户"全优化，但要遵从原著"。① **清占位 POI（6 处）**：隐世小福地/扶摇残城/铁甲大营/金甲王朝/文脉书院群/武庙 全删（原著未明载），相关路网同步收；places 73→67。② **宝瓶洲瓶颈去重叠**：龙泉郡标签左移、落魄山移到(450,248)标签右上、龙泉剑宗挪到(430,228)标签左，三点标签不再互压。③ **fitLayer 全画风**：原先只写实绘卷自适应；改为九洲统一 fitLayer（bbox 含 places+labels+mtn+roads 采样点），青绿等 6 套也自动居中缩放。④ **路名上图**：新增 `showRoadLbl` 图层，按 path 起点渲染 `.road-lbl`；点路时同步高亮。⑤ **拖拽防误触**：attachCont/Poi/Roads 的 go() 全加 `if(moved){moved=false;return;}`。⑥ **POI 热区动态**：按地名字数算宽（`nm.length*11+16`），并加点位 r14 透明命中圆。⑦ **总图装饰山分洲**：WORLD 每洲加 `wMtn[]`，废除全局同一对 mtnS 贴纸。⑧ **检索**：layerbar 加搜索框，输入即列命中（名/tag），点击 `jumpToPlace` 下钻+居中。⑨ **相关洲跳转**：侧栏详情加 ZHOU_RELS 按钮（如宝瓶↔桐叶/婆娑）。⑩ 拼写 `yasehng`→`yasheng`。校验：JS OK、g 配平、banned 全空、moved/road-lbl/fit/search/rels/wMtn 全 PASS。交付 `浩然天下地图.html`。

- **浩然天下地图（本轮·补洲内国/势力分层）**：用户回指先前「缺洲下国的划分」。加 `detail.realms[]` + `showRealms` 图层 + 点击详文 + 检索收录。**仅原著具名 7 处**：宝瓶洲 大骊/青鸾国/彩衣国/老龙城；北俱芦 大源王朝（注明其余九国未载名）；桐叶洲 大泉王朝/北晋（旧）。淡彩 path 边界 + 虚线描边 + 居中地名；绘卷画风下仍叠层。fitLayer 并入 realms 采样。校验：JS OK、g 配平、7 realms 齐、btnRealms/attachRealms/jumpToRealm 在。

- **浩然天下地图（本轮·地点关联人物打通立绘）**：用户"地点下面可以有相关人物，人物就可以与原有游戏中的立绘和介绍打通"。从 `prototype/game.js` CHARS + `briefs.js` 抽 45 人精简索引 `CHAR_IDX`（n/r/p/s/i/img）；立绘相对路径 `prototype/weapp/assets/portraits/<file>.jpg`（40 人有图，5 人首字占位）。`PLACE_CHARS` 映射 32 个地点/势力 → 人物 key（如 lizhu→陈平安/齐静春/宁姚…，luopo→落魄山八人+供奉，yugui→荀渊/姜尚真，daoxuan→余斗）。侧栏地点/势力详情底部渲染人物卡片（立绘+名+身份），点卡片 `showChar` 看大图+境界/所属/简介。校验：JS OK、g 配平、立绘文件 0 缺失。交付 `浩然天下地图.html`。

- **立绘/场景同步（源：qwenworkcn outputs/剑来角色立绘）**：用户"从这里重新同步一次立绘和场景图到项目"。源 132 张（人物 1024×1536 竖版 + 场景横幅）。① **PC 立绘** `prototype/assets/portraits/`：115 人宽 360 jpg q84（约 25KB/张，共 ~5.8MB），旗舰另存拼音别名；地图 CHAR_IMG 改指向此目录。② **weapp 立绘** 先误写主包 `assets/portraits`（宽 180）导致主包 2910KB>2048KB 上传失败。③ **场景** 拷入 `prototype/bg/`：倒悬山/河畔议事/举城飞升/廊桥认主/小镇牌匾/群像长卷/那就打/隐官一脉/青白之争/甲申帐/开天斩周密 等 17 张。④ 「文圣.jpg」另拷为「老秀才.jpg」。仍无图：荀渊/刘景龙/纳兰烧苇/搬山猿/石柔。

- **weapp 立绘分包（修上传超限）**：用户"先分包，可以了就不压缩"。① 立绘迁至 **`packageFulu/assets/portraits/`**（宽 200 q80，121 张）；主包删除 `assets/portraits`。② 路径统一 `/packageFulu/assets/portraits/`：`data.js` artUrl、`sect.wxml`×2、`wushipai.js`×2。③ `gen_data.js` PORTRAIT_DIR 与生成模板同步改。实测：**主包 1.693MB（余 314KB）**、**packageFulu 1.769MB（余 237KB）**、均 <2MB。PC 高清仍走 `prototype/assets/portraits`（地图用）。**铁律复验**：加图必须先分包；分包本身也限 2MB，360px 塞 fulu 会 5.7MB 爆包，200px 是分包内可用上限附近。

- **weapp 四分包 + PC 模块对齐**：用户"立绘单独分包 + 系统其他图片一个分包；把 pc 的其他模块也同步到微信（个人中心、地图入口）；除图片压缩外规则模块一致"。① **分包**：`packageArt`（立绘 1.76MB）、`packageMedia`（sect/shenci/shujianhu/bg 0.76MB）、`packageFulu` 仅符箓页（14KB）、`packageLongque`。**主包 1.10MB（余约 0.9MB）**。路径：`/packageArt/assets/portraits/`、`/packageMedia/assets/{sect,shenci,shujianhu,bg}/`。② **新页**：`pages/profile`（行迹录）、`pages/baofuzhai`（包袱斋四签）、`pages/map`（九洲→要地/势力/路网→人物）。③ **utils**：`profile.js`/`baofuzhai.js`（localStorage→wx storage）、`mapdata.js`（自浩然地图 HTML 抽 ZHOU/CHAR_IDX/PLACE_CHARS）。④ title 加入口；engine `endGame` 埋点 `prRecordBattle`。校验：JS 全 OK、title view 48/48、三页四件套齐、五包均 <2MB。

- **weapp 新页空白修复**：用户截图行迹录全黑；因 `profile.js`/`baofuzhai.js` 从 PC 拷贝时残留 `document`/`render`/`SFX`，小程序 require 即崩。① **重写为纯数据模块**（无 DOM）：境界表/道途/道行埋点；包袱斋和气斋/拣漏/藏书/行囊逻辑。② 页内 `safe()` 包刷新。③ title 新模块（地图/包袱斋/行迹录）**挪到「其他」组末尾**。④ 地图已是原生列表、不依赖大图。⑤ engine 终局同时 `bfAddCoin`。校验：document/localStorage/render 皆 0、title 51/51、新模块在后、主包 1.04MB。

- **weapp 地图 canvas + 包袱斋 SVG + 符箓图**：用户"浩然天下不要文字形式，要地图；包袱斋用上原有 svg；符箓把之前的图片补过来，点击有飞出效果，清晰度够手机即可"。① **地图**：`pages/map` 改 canvas 2d 自绘九洲轮廓（land path Path2D）+ 剑气长城虚线 + 倒悬山 + 点洲下钻画要地/路网/山，面板看人物；无大图。② **包袱斋**：39 枚 `bf-*.svg` 拷入 `weapp/assets/icon`，和气斋/藏书/行囊/摊位列表带图标。③ **符箓**：`prototype/assets/fulu` 21 张压至宽 480 q80 → `packageFulu/assets/fulu`（约 1.2MB）；卡片缩略图 + 点开「符箓飞出」动画（scale/rotate/blur）后进详情。包体：主包 1.09MB、packageFulu 1.15MB、packageArt 1.76MB、packageMedia 0.76MB 均 <2MB。

- **PC 卷首/符箓布局修复**：用户截图卷首入口单列竖排、符箓详情图巨大。git 显示 `index.html`/`game.js` 布局代码未改，但渲染异常。处置：在 `index.html` `</body>` 前追加 **!important 保险样式**——强制 `.mode-grid` 多列网格、`.ts-mode width:auto`、`.fulu-thumb` 150px、`.fulu-art` 240×320 `object-fit:cover`、立绘 max-height。此前微信端资源挪分包已 `git checkout` 恢复主包路径。教训：**别再动 PC 卷首结构；加图先分包且勿改已有路径；用户说「坏了」先恢复再修，不要辩解。**

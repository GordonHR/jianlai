# 2026-08-28

## 剑来卡牌游戏设计（类三国杀）
- 用户想把《剑来》做成类三国杀卡牌对战游戏，立绘在 `C:/Users/Administrator/.qwenworkcn/workspace/msinwe10xb0lim80/outputs/剑来角色立绘`（100+ 张，覆盖核心阵容）。
- 已产出设计文档：`剑来卡牌游戏设计.md`（v0.2）。
- 框架：五阵营（剑气长城/中土文庙/蛮荒天下/青冥天下/莲花天下）+ 散修自由；核心机制=气血+境界+本命飞剑成长+「讲道理」判定。
- 用户要求人物尽量齐全、立绘可慢慢补。莲花天下人物与立绘最缺，已给备选（先四阵营+散修起版）。
- 下一步可选：A 逐角色完整技能表 / B 可运行 HTML 原型 / C 剑来 vs 蛮荒 1v1 demo。
- 用户选 A：已完成「剑来全角色技能表.md」(v0.3)，覆盖全部 ~100 角色（气血/境界/定位/技能），开发可直接照表实现。含实现约定与 v1 卡池建议（先四阵营+散修 36 张，莲花 v1.5）。
- 用户说"你看着办"，遂直接做 B：可运行 HTML 原型。
- 已建 `剑来/prototype/index.html` + `game.js`：单文件热座卡牌原型。选人界面(13 名代表角色)→对战。实装机制：剑气/守心/丹药/问剑/本命飞剑(装备+1)、12 种卡(含论道/万剑归宗/蛮荒入侵/破阵/借物/天劫)、讲道理小游戏(陈平安问心)、各阵营代表技能(陈清都一剑/道祖道法自然/周密算计/阿良问剑/齐静春教化/宁姚剑心/米裕杀力/曹慈佛祖反弹/白也诗剑/礼圣礼法/白泽旁观)。
- 注意：本机 node 无执行权限，无法跑 `node --check`，仅人工复核语法。用户需在浏览器打开 index.html 试玩。
- 后续：试玩后调数值 / 接真实立绘 / 扩角色到全量 / 做 AI 对手。

## 立绘 + 特效增强（回应"立绘呢？特效呢？这么简陋吗"）
- 已把 13 张立绘拷贝进 `prototype/art/`（文件名用角色 key，如 chenpingan.png），选人/对战界面都用 `<img>` 引立绘，`onerror` 缺失自动隐藏。
- 加了水墨渐变底色 + 阵营色辉光（CSS `color-mix`），角色头像圆形描边阵营色，当前行动者脉冲动画。
- 特效：斩击（#slash 全屏斜光，按攻击方阵营色）、受击（红闪+位移抖动+屏幕 shake+飘 -N）、治疗（绿光+飘 +N）。
- 关键 bug 修复：原 `loseHp` 先 `render()` 再 `fxHit`，而 render 重建 DOM 会把动画 class 清掉，特效几乎不可见。改为「特效入队 pendingFx → 最后一次 render() 后才 flushFx() 播放」，并把 `.player` 改 `overflow:visible` 让飘字不被裁切。

## 人物补全（回应"人物补全，没立绘的空着就行"）
- 用 Python 解析 `剑来全角色技能表.md`，重写 `game.js` 的 `CHARS`：保留 13 旗舰角色（带精细技能逻辑 + `flagship:true`），其余 ~84 角色按阵营生成，共 97 角色入选人池。
- 每个非旗舰角色挂一个**阵营天赋**被动（仅非旗舰生效，保证 ~100 角色都有可玩特色）：剑气长城=锋芒(剑气+1)、中土文庙=耕读(摸牌+1)、蛮荒=嗜血(首伤回1)、青冥=无为(首伤-1)、莲花=慈悲(丹药回2)、散修=机缘(首锦囊摸1)。接在 startTurn/resolveAttack/applyDamage/loseHp/doHeal/playTrick。
- 无立绘角色：选人/对战界面用「阵营色块 + 名字首字」占位（imgwrap/pwrap + .ph），`onerror` 隐藏真图。补图只需按 key 命名丢进 `art/`（旗舰用拼音 key，其余用中文名 key，如 `art/左右.png`）。
- 对战界面每个角色下方显示其技能名（pskills）；非旗舰角色技能区有「天赋·XX」说明按钮。
- 校验：node 在本机 noexec 无法 `node --check`，改用 Python 统计括号配平（280/280、540/540、166/166 平衡）；生成器脚本 `_gen_chars.py` 跑完已删。


## 13:27 立绘/技能区微调
- 选人卡 .imgwrap 244->300px（立绘更完整）；.skills 固定 height:96px + overflow-y:auto（技能说明不再自适应高度）。
- 对战：对手 .pwrap 118->150px；英雄立绘 150x210->160x250；.pskills max-height:40px、.pskills.big max-height:72px，均 overflow 限制（防卡片高度失控）。


## 13:35 增加可玩性（剑来本味，非三国杀套用）
- 境界突破：斩杀对手升一阶境界(练气→止境→飞升)且气血上限+1（game.js applyDamage+breakthrough）。
- 同阵营齐心：场上有同阵营存活友方时剑气伤害+1；卡面加「⚔齐心」标签（resolveAttack+hasAlly）。
- 新增剑来本味锦囊：读书(摸2)、炼器(祭本命飞剑)，替代原想加的三国杀卡名。
- 阵营用艺术字(.fac-over 草书)浮于立绘左上；技能说明区高度 96→70px（防失控固定高度）。
- 用户约定：不要照搬三国杀卡牌规则/卡名（如桃园结义不合适），机制可借鉴但命名与风味须贴合剑来世界观。

## 13:41 修复选人界面「好几个角色没技能」
- 原因：`renderSetup` 用 `parseSkills(c.txt)` 解析，正则只匹配 `【技能名】描述`；前 13 名旗舰角色 `txt` 用的是 `技能名:描述` 格式，导致解析为空。
- 修复：把 `chenpingan/caoci/ningyao/aliang/miyu/chenqingdu/baiye/qijingchun/lisan/zhoumi/baize/daozu/fozu` 的 `txt` 全部改为 `【】` 格式，并与 `剑来全角色技能表.md` 对齐。
- 校验：`game.js` 语法通过 Function 构造器解析（本机 node 无执行权限，用 D 盘 Node）。

## 13:44 选人卡技能区去滚动条，改为悬停提示
- 用户反馈：技能区带滚动条太丑，「不要有这种滑动的，如果展示不全，鼠标放上去展示就行了」。
- 修改：
  - `index.html`：`.skills` 由 `height:70px; overflow-y:auto` 改为 `max-height:70px; overflow:hidden`；新增 `.skilltip` 绝对定位悬浮层；`.char:hover` 时 `overflow:visible; z-index:10` 并显示提示。
  - `game.js`：`renderSetup` 同步生成 `.skilltip` 完整技能文本（与 `.skills` 截断显示对应）。
- 效果：选人卡常态只展示前 3–4 行技能摘要，无滚动条；鼠标悬停卡面时从底部浮出完整技能说明。


## 15:13 人物简报弹窗视觉强化
- 弹框加宽 860->1100px；左栏 300->380px，立绘 max-width 260->340px。右栏 overflow 改 visible（去滚动条）+ padding 收紧。
- 新增立绘环绕特效(CSS)：portrait-aura 呼吸光晕、portrait-halo 旋转剑气阵环(外11s/内7.5s反向，conic-gradient+mask 环形)、portrait-spark 7颗上升灵气光点(JS 生成，位置/延迟错开)、portrait-mirror 地面反光、portrait-img 悬浮+边缘呼吸光晕。
- 蒙版朦胧背景：#modal::before 用 --blurbg 铺 blur(28px) 放大的角色立绘，::after 暗色径向渐变压暗；JS 用 pendingBlur 注入 URL。


## 15:30 崔瀺/崔诚拆分 + 弹框固定 + 卡片按钮贴底 + 补简报 + 名字流光
- **崔瀺/崔诚拆开**：CHARS 原崔诚（崔瀺本体）拆为独立崔瀺(国师绣虎，飞升，气血4) 与崔诚(少年崔东山分身)。art 已复制两份立绘。BRIEFS 重写两条；陈平安师承改为[齐静春,文圣]、亦师亦友[崔诚]；文圣弟子改为[崔瀺,齐静春,陈平安]；持剑者宿敌改为[崔瀺]。
- **人物简报按钮固定底部**：.char flex column min-height:430px；.charinfo flex:1 display:flex flex-direction:column；.sk-more margin-top:auto（无论技能多少都贴底）。
- **弹框大小固定**：.brief-modal min-width:1000px / height:min(640px,86vh) / min-height:580px（内容少也不缩小）。
- **补 BRIEFS**：从 13 旗舰补到 50+ 角色：崔东山/左右/魏晋/董三更/姚近之/齐廷济/齐景龙/隋右边/陆芝/君倩/周澄/李宝瓶/林守一/宋集薪/杨老头/刘重润/之祠/斐然/龙君/余斗/陆沉(道老二)/道老三/玉圭/阿弥陀佛/多宝/莲荷/菩萨/阮秀/火龙真人/裴钱/刘羡阳/李二/李柳/朱敛/曹晴朗/姜尚真/姜赦/崔东山/马苦玄/于玄/宋雨烧/沛湘/周海镜/周米粒/稚圭/黄庭/柳柔/陆台/长命/贺小凉/谢松花/谢狗/郑居中/郭竹酒/徐远霞/卢白象/吴霜降/苏心斋/刘灞桥/小陌/苏稼/裴杯/陈景清/陈暖树/隋景澄/魏檗/魏羡/赊月/南簪。
- **名字题头流光**：.brief-mast 加::after 金色横线流光(mastShine 动画)；.brief-name 改为文字渐变 + drop-shadow。


## 16:20 「更加游戏/武侠」整体重构（index.html + game.js 双文件重写）
- **游戏性**：
  - 新增 **AI 对手**：开场卷轴选「仗剑独行 / 群雄论剑」两种模式；AI 模式玩家选 1 人，系统按宿敌关系（HOSTILE 表，蛮荒天下为公敌）撮合 1–3 名对手。AI 自行出牌、选目标、应招（出守心/弃牌/讲道理），有 `aiPickCard` 优先级（装备→残血回血→群伤锦囊→进攻锦囊→问剑接剑气→剑气→补牌锦囊）。
  - 人类与 AI 共用 `playCard`/`useSkill`，分叉只在 `isAI()` + `ask()` + `chooseTarget()` 三处。
  - 新增**剑气次数限制**（基础 1/回合，本命飞剑 +1，米裕/齐廷济 +1），手牌区实时显示「剑气 0/1」；修掉了原版「出剑气不弃手牌」导致的无限攻击 bug。
  - 三阶段回合流（摸牌→出牌→弃牌）+ 顶栏 stepper；弃牌阶段超出上限会弹多选弃牌窗。
  - 宣纸质感真卡面（92×132，卡头/中央大字/点数角标/类别条），手牌按索引做扇形排布。
  - 战绩结算页（斩杀/造成/承受/疗愈）+ 再战；WebAudio 合成音效（剑鸣/命中/治疗/回合钟/突破/胜负，可开关）。
- **武侠感**：卷轴开场题字+剑光横扫、回合横幅（草书大字+剑光扫过）、出牌招式飘字（一剑霜寒/守心如玉/万剑归宗…）、剑光轨迹（攻→守飞行「剑」字）、灵气粒子背景、战报文言化、结算页立绘。
- **平衡修正（跑 24 局×4 人数采样得出）**：同阵营齐心改为「目标须为外敌 + 存在第三名同阵营存活者」；锋芒改为每回合首次 +1 且在守心判定后才结算。否则 2 人同阵营局 1 回合互秒。现中位回合数 2人5/3人7/4人10/5人12。
- **Bug**：重写漏掉 `afterAction()` 定义，首次出牌必崩 ReferenceError（已补）。
- 选人页新增阵营筛选 + 搜索框 + 底部已选阵容条（因 98 人卡池太长）。

## 15:32 崔诚/崔瀺/崔东山三独立 + 卡片高度统一
- 用户认知：崔诚=崔瀺之祖(崔家上代)，崔瀺=绣虎国师，崔东山=崔瀺少年分身，三个独立人物。已重写 CHARS 与 BRIEFS 三条；陈平安亦师亦友改为崔东山(原误为崔诚)。
- 卡片高度仍不齐原因：上次 .char 的 flex+min-height Edit 实际未生效（被 linter 标记），刚补上：.char min-height:450px display:flex flex-direction:column；.charinfo flex:1 display:flex flex-direction:column；.sk-more margin-top:auto。

## 17:09 选人卡/弹框/简报/随机特效/书简湖问心局
- **境界/阵营冲突修复**：选人卡 `.badge-realm` 从左上角移到右上角，与左上角阵营标签 `.fac-over` 分离，避免重叠。
- **人物弹框重做**：
  - 立绘上层叠加阵营/境界/气血徽章（与选人卡一致）。
  - 技能区只列技能名（chip 标签），不再展开完整效果文本。
  - 弹框蒙版背景加大模糊、放大、加亮，增强气势。
- **人物简报数据清理**：用 Node 脚本去重 briefs.js，删除 8 条重复条目（左右/李宝瓶/龙君/陆沉/阮秀/裴钱/刘羡阳/崔诚）；为所有 CHARS 角色补齐 BRIEFS；新增 李槐、搬山猿 两名角色及对应技能/简报。
- **随机择主特效**：选人界面底部「随机择主」按钮，点击后可见卡牌快速翻转/飞转并减速落定，最终自动选中目标。
- **书简湖问心局**：新增独立叙事小游戏（非卡牌对战），玩家替陈平安在书简湖恩怨中做 7 道道德抉择，影响「情/理/心结/道心」四值，最终触发多结局（碎文胆·问心无愧 / 徇私护短 / 法理无情 / 余波未平）。

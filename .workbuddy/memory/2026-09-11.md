# 2026-09-11 工作日志

## 戏里戏外（tales.js）定位修正：角色故事归位个人简介
- **用户明确方向**："特别的故事如果跟本身角色有关系，直接放到个人简介做故事集"。即——角色专属故事进 `BRIEFS.scenes`（故事集），`tales.js`（戏里戏外）只留世界/事件级跨角色大场面（书简湖问心局、剑气长城末战、倒悬山、老龙城、三教散道、五彩天下等）。
- **本轮动作**：把上一轮写进 `tales.js` 的 4 篇角色故事（刘羡阳/顾璨/李宝瓶/李槐）的核实情节，**充实进对应角色的 `BRIEFS.scenes`**（原为 1–3 条概述 → 现 4 条硬情节）。双端 `briefs.js` + `weapp/utils/briefs.js` 同步（PC 单引号 / weapp 双引号 JSON 风格），用 `_enrich_briefs.cjs` 按"角色 key→定位 scenes 数组→结构替换"原子改写。
- **纪律**：quote 仅收确证原句（顾璨 scene q「我死，顾璨都不能死。」/ 李宝瓶 scene q「有些人心如花木，皆向阳而生…」）；刘羡阳/李槐无确证原句 q 留空。
- **`tales.js` 4 篇未删**：用户此前批过"别乱搞/越界"，故保留已批准的戏里戏外内容，不 undo。两头各司其职。
- **验证**：`node --check` 双端 0 错；解析确认 4 人 scenes 各 4 条、新标题就位。

## 待续（按新规则）
- 落魄山编内（裴钱/崔东山/郑大风/周米粒/朱敛/姜尚真/陈暖树）：角色侧面 → 各人 `BRIEFS.scenes`。
- 名场面事件：书简湖问心局/倒悬山/老龙城/剑气长城末战/三教散道/五彩天下 → `tales.js`（事件级）。
- 其他高光（白也/米裕/董三更/于樾/刘景龙/荀渊/谢时/蛮荒侧）：角色相关 → `BRIEFS.scenes`；跨角色大事件 → `tales.js`。


## 全部继续批次交付（2026-09-11 续）
- 门禁修复 tales 断言：TALES.length!=9 改 <9，冒烟 23/0。
- 石柔双端自洽（方案①）：weapp data.js CHARS+SKILLS 补石柔；_sc_keys 豁免清空，门禁 0 差异。
- 名场面 6 篇 tales.js 双端（书简湖问心局/倒悬山/老龙城/剑气长城末战/三教散道/五彩天下），均 WebSearch 核查节点，quote 留空防臆造。tales 现 19 篇。
- 董三更 scenes 补双端：长城末战剑宰荷花庵主（WebSearch 确证）。
- 待用户决：白也/米裕/刘景龙/于樾/荀渊/谢时 完全未建入卡牌体系，补 scenes 须先加四表双端，未擅加。
- 验证：双端 node --check 0 错；_sc_keys 0 差异；_sc_smoke 23/0；临时脚本已删。

## 符箓图鉴（fulu）模块 · 双端落地（续）
- 新增独立符箓图鉴 `prototype/fulu.js`（PC 阅览模块，与 wushipai/戏里戏外平行），17 种原著符箓：缩地/阳气挑灯/宝塔镇妖/破障/静心安宁/祛秽涤尘/山河剑敕/求雨/起火烧煞/天部霆司/大江横流/撮壤/白泽路引/剑气过桥/三才兵符/五雷正法/镇剑。
- 四表纪律：desc/caster/src 全部依联网核实的原著与古法符箓体例，未编造；图形原著未实绘，标注"据古法符箓体例·云篆·符头符胆符脚·朱砂立意重构，非原著实绘"。
- 双端同步：weapp `pages/fulu/`（4 件套 js/wxml/wxss/json）纯文字复用同一 FULU，不引图（主包仅富余 ~148KB）。
- PC 接线：game.js `state.phase==='fulu'` 分派(L3129)、主菜单入口(L3405)、`function openFulu`(L3487)；index.html 引 `fulu.js?v=20260911a` + CSS；双端 `mode-fulu.svg` 图标（金色细线符纸云篆）。

## 关键纠正（用户）
- 用户指出此前生成的"符箓"实为山水/火焰/剑光**意境画**，不是真符箓。**真符箓 = 黄纸·云篆（符头·符胆·符脚）·朱砂·朱印·敕令笔势的书法/符文**，非风景。已按真符箓体例重做并出样稿（山河剑敕符）确认后补齐 17 张。
- 已写入 MEMORY.md §7 符箓配图铁律，含 ImageGen 去水印转 jpg、逐张串行生成（并行秒级重名互相覆盖）、英文 prompt 规避敏感词拦截等坑。

## 配图处理
- PC 端 `prototype/assets/fulu/*.jpg`（17 张，3:4 云篆符箓），Pillow 去右下角 `AI生成 WORKBUDDY` 水印并转 jpg。
- 临时脚本 `prototype/_fulu_process.py` 与本批次 generated-images PNG 已清理。
- 验证：17 个 `<id>.jpg` 与 fulu.js FULU 的 17 个 id 完全对齐；双端 FULU id 序列一致。

## 主菜单卡片标题折行修复（用户报"空格那么大，四个字都要折行"）
- **根因**：`.ts-mode h3` CSS 有 `letter-spacing:6px`，而 `人物志/戏里戏外/无事牌/符箓图鉴/包袱斋/行迹录` 六个卡片标题**在 markup 里还带字面空格**（如 `<h3>符 箓 图 鉴</h3>`）→ 双重间距；4 字标题宽度 ≈174px 超过 h3 可用宽 ≈170px（卡 250px − padding36 − icon34 − gap10）→ 折行。经营/对战组多数标题无字面空格，故正常。
- **修法**：去掉这 6 个卡片标题的字面空格（保留 CSS `letter-spacing:6px` 统一疏排）→ 与无空格标题（落魄山/群雄论剑）及 6px 的 `.ts-group-title` 一致，4 字不再折行。
- **同步门禁**：`_sc_smoke.cjs`（断言 `<h3>无 事 牌</h3>`）、`_pf_int.cjs`（标题屏查 `行 迹 录` ×2）已改新串。
- **未动**：详情页 `h1`（codex-head，含字面空格，但 h1 宽裕不折行，用户未提）；微信端 title.wxml 同类卡片（用户只报 PC）。
- **验证**：`_sc_smoke` 23/0；`_pf_int` INT ALL PASS；`_pf_smoke` ALL PASS；`_sc_keys` ALL_KEYS_OK(A=B=127)；game.js 语法 0 错。
- **坑**：`_pf_smoke.cjs` 用相对路径读 `profile.js`，**必须在 prototype/ 目录下作为 cwd 运行**，否则 ENOENT。`_pc_combo.cjs` 已不存在（记忆过时）。

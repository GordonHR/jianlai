# 《剑来》游戏 · 可玩性与界面效果增强方案

> 出案：灵犀 · 2026-09-09 · 版本 `prototype/index.html?v=20260902g` 之后
> 范围：PC 网页端 + 微信小程序端，覆盖 对战 / 落魄山 / 山水祠 / 书简湖 / 包袱斋 / 行迹录 六大模块
> 本文只出方案，不动任何生产文件。选定后按「先出预览、再同步两端」的规矩落地。

---

## 0. 先说判断（三句话）

1. **现在不缺机制数量，缺的是"连通"和"变化"。** 六个模块是六座孤岛——打完一局赢了，钱进了包袱斋；但落魄山辛辛苦苦经营五年、山水祠熬过四十八旬，对别的模块**一文不值**。玩家没有"我这一局的付出在别处有用"的感觉，重复可玩性就全靠 IP 情怀硬撑。

2. **界面效果上，PC 端已经很满了**（30+ 组 keyframes、18 条实时合成音效、处决全屏、连携播报），继续堆特效**边际收益递减**。真正的缺口是三件事：**小程序主包已经爆了（超 2MB 52,862 字节，什么都加不进去）**、**小程序跨端不对等（无声、处决只闪 0.2s）**、**AI 快出牌时特效互相覆盖，反而看不清发生了什么**。

3. 所以这个方案的重心是 **一条闭环 + 一个随机源 + 一次瘦身 + 一轮反馈节流**，而不是再加十个特效。按你"贴合原著、不做阵营调平"的边界，下文每一条都会写清它**制造了什么新决策**，说不清的一律不提。

---

## 1. 现状体检

| 模块 | 现状 | 主要缺口 |
|---|---|---|
| **对战 game.js** | 120 角色/120 技能、判定/势压/本命飞剑/连携、四模式、PC 视听双通道 | 局内无随机源（每局同构）；本命飞剑无差异；AI 三档难度只是资源差；无新手引导 |
| **落魄山 sect.js** | 6 幕 54 选点 17 结局、因果引擎（道契/剑礼/守/势） | 结局即终点，产出不带出模块；"第五年飞升之争"是游戏原创、与原著气质拧 |
| **山水祠 shenci.js** | 48 旬、五维资源（香火/气数/阴德/威灵/香客）、脱根机制 | 同上，孤岛；跨局无积累 |
| **书简湖 shujianhu** | 问心局叙事 + 8 张插曲图 | 同上 |
| **包袱斋 baofuzhai** | 神仙钱货币、赊欠、掌眼、31 枚拟物图标、**悟道印记可带入对局** | **目前唯一存在的跨模块闭环**（对战→钱→买货→增强下一局），但只有这一条，且是单向 |
| **行迹录 profile.js** | XP/等级/境界说明/存档 | 只记录，不解锁任何东西 |
| **小程序端** | 引擎与 PC 1:1 | **主包 2,150,014 B > 2MB**；SFX 全 no-op（无声）；处决 0.2s |

**一句话**：骨架和血肉都有了，缺的是**经络**（模块互联）和**心跳**（局内变化）。

---

## 2. 三个硬约束（方案的地基）

| 约束 | 现状 | 对方案的含义 |
|---|---|---|
| **小程序主包 2MB** | 已超 52,862 B，无分包 | **任何"加图/加音效"的加法，都必须先做瘦身或分包**。这是 P0 前置，不是可选项 |
| **双端一致成本** | PC/小程序每次改动要落两处（js + wxml/wxss） | 纯数据类改动（如天时表、飞剑 subtype）成本最低；视听类改动要双份 |
| **原著纪律** | 人物/技能/飞剑不可乱编，须标 src | 新机制优先从原著已有意象里长出来（账房、天时、问剑、讲道理、铸剑），不新造设定 |

---

## 3. 可玩性增强（六项）

### A. 「一本账」跨模块元进度闭环 —— P0，最大增量

**做什么**：建一个统一存档 `jianlai_account`，把六个模块串成一条经济链。

```
对战胜利 ──雪花钱──▶ 包袱斋（买法宝/印记）──增强──▶ 下一局对战
    │                                                      
    └──声望/行迹 XP──▶ 行迹录 ──解锁──▶ 角色 / 天时 / 难度 / 事件
                                                     
落魄山年结算 ──金精铜钱·人情──▶ 账房 ──▶ 买山头 / 修缮 / 添置
山水祠旬终 ──香火折算功德──▶ 点亮心香 ──▶ 兑换一次性护持
```

**新增什么决策**：玩家每局结束要回答一个真问题——**这笔钱是现在买法宝让下一局更强，还是攒着在落魄山买下第二个山头？** 这是目前完全没有的取舍，也是"再玩一局"最硬的理由。

**原著依据**（很硬，不是硬编）：
- 陈平安是落魄山**账房先生**、韦文龙管账——"账"本身就是《剑来》后半部的骨架。
- 包袱斋**可赊欠历来定例**（已写进游戏），债务可跨模块。
- 山水祠：陈平安后期**遍访山水神灵、以全部功德换取众神点燃心香**（shenci.js 开篇已引）——功德天然可以折算成通用资源。
- 落魄山：三袋金精铜钱买山头（已有设定）。

**落地**：新增 `account.js`（存档 + 汇率表 + 解锁树），各模块在结算点调 `ACC.earn('coin', n, '对局·胜')`。包袱斋的 `BF.coin` 迁移为账房子账户。
**工作量**：M。**风险**：中（要动存档版本，需旧档迁移，与 `BF_VER` 迁移同样的写法）。

---

### B. 「天时 · 地势」局内随机环境 —— P1，性价比最高

**做什么**：每局开局翻一张**天时牌**（12 张，全部取自原著意象），给全场一条全局规则，并附带一个中立事件。

| 天时 | 效果示例 | 原著出处 |
|---|---|---|
| 夜游 | 判定 +2；魏檗在场则额外摸一张 | 魏檗神号"夜游" |
| 大旱 | 丹药回复 −1；香火类技能 +1 | 山水祠旱灾事件 |
| 风雪 | 剑气 −1，守心 +1 | 剑气长城风雪 |
| 蛮荒压境 | 每回合开始所有角色 −1 气血上限（不致死） | 蛮荒天下入侵 |
| 文庙大考 | 中土文庙 / 儒家角色判定 +2 | 文庙 |
| 骊珠洞天降格 | 所有角色手牌上限 −1 | 骊珠洞天 |

**新增什么决策**：同一手牌在不同天时下打法完全不同——**要不要为了配合天时改选角色？起手要不要留守心？** 直接解决"每局都差不多"。

**落地**：纯数据表 `OMENS[]` + `startGame` 里翻牌 + `swordDamage` / `judgePhase` 里挂全局修正。**零新增美术资源**。
**工作量**：S。**风险**：低（不动核心循环，可加 `state.omen=null` 一键关闭）。

---

### C. 本命飞剑差异化 · 铸剑 build —— P1

**做什么**：每把飞剑带一个 sub-type，让"铸哪把剑"变成 build 决策，而不是当前的"装上就 +1/+1"。

| 飞剑 | subtype | 效果 | 原著 |
|---|---|---|---|
| 笼中雀 | 困 | 命中后目标**本回合不可守心** | 陈平安本命 |
| 井中月 | 群 | 溢伤溅射相邻一名 | 陈平安本命 |
| 初一 | 拘 | 击杀后夺其一张手牌 | 赠剑胚 |
| 十五 | 速 | 额外出剑不消耗酒 | 赠剑胚 |
| 天真 | 净 | 无视装备减伤 | 宁姚 |
| 饮者 | 醉 | 命中后目标下回合判定 −1 | 阿良 |

**新增什么决策**：**这局是打控制（笼中雀）还是打爆发（井中月）？** 铸剑从"有就装"变成"选一把"。
**落地**：`FEIJIAN[]` 表加 `sub` 字段，`forge` 技能与 `resolveAttack` 读 subtype。小程序端同步。
**工作量**：M。**风险**：中（改伤害链路，但 `swordDamage` 已是单一真相源，改动集中）。

---

### D. 「讲道理」对决 —— P2，IP 最大爽点

**做什么**：把判定阶段升级为可选的**讲道理**：双方暗置一张手牌，比"点数 + 道理值"（道理值 = 读过的书 ×N + 文庙/儒家 +N + 包袱斋读过书目加成）。赢家夺对方那张牌，或令其本回合不得出剑。

**新增什么决策**：**这张牌是留着出剑，还是留着讲道理？** 把"讲道理"从背景设定变成可操作的资源博弈——这是《剑来》最核心的招牌，现在完全没做出来。

**落地**：判定阶段加一个可选动作 `argue()`；道理值可复用包袱斋 `BF.read` 书目数（顺带让"读书"这个锦囊更有意义）。
**工作量**：M。**风险**：中高（新交互，需双端 UI）。

---

### E. AI 难度质变 —— P1

现在三档难度只差起手牌数和 Boss 血量，"困难只是资源少"。改成：

| 难度 | 行为 |
|---|---|
| 简单 | 固定 25% 失误率：有斩杀线不打、留牌不用、装备延后 |
| 普通 | 现状（集火必杀 / 连携 / 优先 flagship） |
| 困难 | **会蓄爆**（屯够一套连携再爆发）、**会骗牌**（先用小剑气逼你守心）、**针对玩家**（优先打血线最低的人类） |

**新增什么体验**：难度从"数值差"变成"对手性格差"。工作量 S–M，风险低（纯 AI 分支，`_sc_battle.cjs` 可 A/B 验证）。

---

### F. 新手引导局 —— P2

第一局强制走一遍「摸牌 → 判定 → 出剑 → 锦囊 → 弃牌 → 铸剑」，每步高亮当前该点的区域 + 一句话解释，**可跳过但默认开**。另加：手牌全灰时明确提示"为什么不能出"（出剑次数用尽 / 距离不够 / 需要目标）。

**新增什么体验**：把前 3 局的迷茫期压到 1 局。工作量 M（PC + 小程序双份 UI）。

---

## 4. 界面效果增强（八项）

> 前提：**§4.1 瘦身/分包不做完，4.2 之后全做不了。**

### 4.1 小程序分包与资源瘦身 —— P0，前置

| 动作 | 预期收益 |
|---|---|
| 拆分包：主包只留 title / setup / game；**sect / shenci / codex / shujianhu / wushipai 全进分包**，资源跟随 | 主包立减 ~700 KB |
| portraits 493 KB → **转 webp q70 / 或统一 360px 宽** | 再减 ~200–250 KB |
| 叙事图（sect 298K / shenci 358K / shujianhu 107K）迁至分包 | 已含在上一条 |
| 远景：远程资源走 CDN（需配 downloadFile 域名白名单） | 解锁"随便加图" |

**做完之后主包预期 ~1.1–1.2 MB，富余 800 KB**，后面所有加法才有地方放。

### 4.2 小程序音频 / 触觉补偿 —— P1

- `wx.vibrateShort`：出剑轻震、处决重震、被击短震（零资源，立即见效）。
- 极简 `InnerAudioContext`：复用 PC 那套合成音的**思路**，预生成 6 条（出剑/处决/治疗/判定/连携/胜利）短音频，总预算 ≤ 60 KB。
- 处决 `flash-fx` 驻留 **0.2s → 0.9s**，与 PC 的 1.15s 对齐。

**新增什么体验**：手机上"出剑有声音、斩杀有分量"，跨端体验对等。

### 4.3 特效节流队列 —— P1（不是加特效，是让特效看得清）

现在 `shout` / `banner` / `execfx` 直接 `innerHTML` 覆盖，AI 快出牌时**招式喊声和处决会被下一张牌冲掉**。改成：

- 全局一个**演出队列**，同屏最多 1 个喊声；
- **连携 / 处决 / 境界突破强制插队 + 延长驻留**；
- 其余特效在高速模式下自动降级为飘字。

**新增什么体验**：看得清"刚才发生了什么"。这是目前反馈链路里最实的一个洞。

### 4.4 动态战场背景 —— P1（与 §3-B 天时配套）

天时牌翻出后，战场背景实时变化（夜游=星夜 + 灯笼；大旱=焦土裂纹；风雪=飘雪 + 远景模糊；蛮荒压境=血雾压边）。PC 用 CSS 分层 + 粒子，小程序用**一张压缩底图 + CSS 遮罩**（≤8 KB/张，走分包）。

**新增什么体验**：天时从"文字提示"变成"看得见的环境"，沉浸感直接上一个台阶。

### 4.5 出剑定向轨迹 + 剑鸣 —— P2

从攻击者卡片到目标卡片画一条**飞剑轨迹**（金色细线 + 尾迹 + 命中处剑光炸开），替代现在"卡片闪一下 + 飘字"。PC 已有 `slash` 粒子，改造为定向即可；小程序用 `transform` 动画 + 1 条音效。

### 4.6 大立绘入阵 / 阵亡水墨演出 —— P2

角色登场时**水墨晕染显影**（0.6s），阵亡时**褪色 + 墨迹扩散 + 一声剑鸣**。复用已有的 `die` 动画骨架加墨迹层。注意：`onerror` 兜底保持，无立绘角色不硬塞。

### 4.7 章回体战报 + 结算卷轴 —— P2

- **战报改章回体**：每回合自动生成小标题（如「第七回合 · 雨夜问剑」），回合标题由「天时 + 本回合关键事件」拼出。IP 感极强，成本极低（纯文本生成）。
- **结算页改卷轴**：评书式总结（"是役，陈平安三剑斩落蛮荒大妖，连携七次，未曾一败"）+ 数据（最长连携 / 处决数 / 天时 / 斩获雪花钱）+ **朱砂印落款盖章动画**。

### 4.8 演出强度设置 + reduced-motion —— P2

设置项「演出：关 / 弱 / 强」，持久化；`prefers-reduced-motion` 时自动降到"弱"，并把全屏层（`#shout` / `#execfx` / `#winfx` / confetti / swordrain）全部纳入降级范围（现在只覆盖了 `.char` 等少数选择器）。

### 附：两个便宜的小亮点（可随手加）
- **标题页随时辰变化**：白天/黄昏/夜晚三套光色（包袱斋已有昼夜掌柜逻辑，可复用同一个 `bfIsNight()`）。
- **模块间水墨转场**：从对战切到落魄山时，墨迹自左向右扫过再散开（0.5s）。

---

## 5. 路线图（三批）

| 批次 | 内容 | 为什么是这批 |
|---|---|---|
| **第一批（解锁 + 最大增量）** | 4.1 分包瘦身 → A 一本账闭环 → B 天时系统 → 4.3 特效节流 | 瘦身是地基；闭环和随机源是可玩性的两个真缺口；节流是反馈洞 |
| **第二批（深度 + 对等）** | C 飞剑差异化 → E AI 难度质变 → 4.2 小程序音频触觉 → 4.4 动态背景 | 让"每局不同"和"手机端也好玩"成立 |
| **第三批（打磨 + 爽点）** | D 讲道理对决 → F 新手引导 → 4.5/4.6/4.7/4.8 演出层 | IP 爽点与可达性收尾 |

**我的建议**：先做第一批的 4.1 + A + B。做完这三件，这游戏会从"能玩"跨到"想再来一局"。

---

## 6. 我不建议做的（红线）

| 不做 | 理由 |
|---|---|
| 阵营数值调平 | 你已明确现阶段无阵营概念；300 局数据显示胜率差异来自单卡方差，不是阵营机制 |
| 继续堆新特效（不加节流） | 特效已经在互相覆盖，再加只会更看不清 |
| 新增原创人物/飞剑 | 违反原著纪律；要增强优先从已有 120 人里挖 |
| 联网对战 / 排行榜 | 原型阶段无后端，收益远小于成本 |
| 改 sect.js 的"飞升之争" | 应改（原著气质拧），但属另一条线，与本次可玩性增强不冲突也不重叠，单独立项 |

---

## 7. 验收口径（每条都要能证伪）

| 项 | 怎么算做成了 |
|---|---|
| 4.1 瘦身 | `du weapp` 主包 ≤ 1.4 MB，小程序真机能完整跑通六个模块 |
| A 一本账 | 打通"赢一局 → 包袱斋余额增加 → 买法宝 → 下一局生效"全链路；落魄山结算能产出并被账房收到 |
| B 天时 | `_sc_battle.cjs 60` 跑通，局间天时分布均匀，中位回合数不显著偏离 15–18 基线 |
| C 飞剑 | 对战中不同飞剑产生**可观测的不同结果**，且 PC/小程序一致 |
| 4.2 音频触觉 | 真机能感到震动、听到出剑声 |
| 4.3 节流 | 60 局采样中，连携/处决播报**零被覆盖**（用 `_pc_combo.cjs` 加断言） |
| 全部 | `_sc_keys.cjs` / `_sc_smoke.cjs` / `_wx_probe.cjs` / `_pf_smoke.cjs` 全绿，无回归 |

---

## 8. 下一步

你挑一下：
1. **先做哪一批**（我建议第一批）；
2. 或者**先挑一两项出预览**——按老规矩我会做成独立的 `_*_preview.html`，**不动任何生产文件**，你看过之后再决定要不要同步到两端。

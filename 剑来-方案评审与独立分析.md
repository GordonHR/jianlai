# 剑来 · 第三方方案评审与独立分析

> 评审对象：《剑来》项目整体评估与改进方案（另一模型产出）
> 评审方式：逐条对照项目**真实代码与文件**核实，不凭印象
> 日期：2026-09-18

---

## 一、总体结论

**七成可用，产品判断力不错，但有 4 处事实硬伤会导致优先级排错。**

它把"小程序加 wx.vibrateShort 触觉"排在第一批去做——但项目里 `wx.vibrateShort` 已经在 5 个模块里实现了；它把"存档迁移风险"轻描淡写成一句"风险：中"，而这恰恰是**唯一不可逆**的那一步。

如果照它的批次表执行，你会先做一件半成品收尾的事，而把真正的结构性风险留到后面。

---

## 二、逐条事实核查

| 它的断言 | 核实结果 | 证据 |
|---|---|---|
| 门禁齐全（`_sc_keys`/`_smoke`/`_battle`/`_wx_probe`/`_pc_combo`） | **结论对，名单错**。前 3 个存在，`_wx_probe`/`_pc_combo` **全库不存在** | 实际常驻：`_sc_keys` `_sc_smoke` `_sc_battle` `_narr_smoke` `_pf_smoke` `_lq_smoke` `_map_verify` `_wx_size_guard` `_port_ref_guard` |
| 120 角色技能 | **偏了**：卡牌池 **108**（人物简报侧 127） | `game.js` CHARS 计数 108；`data.js` 127 keys |
| PC 有约 18 条合成音效 | **过时**：现 **24 条** | `game.js:538` SFX，WebAudio `tone()`+`noise()` |
| 小程序无音效 | **准确** | `weapp/utils/engine.js:27` `heal(){} die(){} win(){}` 全为空函数 |
| packageArt 仅余约 117KB | **过时**：现余 **295KB**（1752.7KB / 86%） | 2026-09-18 已清 13 组冗余立绘 + 修正 preloadRule |
| 「处决「斩」驻留约 0.2s」 | **PC 端错**（PC 实为 **1.15s**，白闪 0.34s）。若不指明端，与它自己建议里的"对齐 PC 0.8–1.0s 量级"自相矛盾 | `index.html:667` `#execfx.go { animation:execHold 1.15s }` |
| 难度三档只调资源、不改 AI 策略 | **准确** | `game.js:1164` DIFF 仅 `allyDraw/foeDraw/bossMul` |
| 本命飞剑无 subtype 差异 | **准确**：有 `sub` 字段但全角色共用唯一值 `'feijian'` | `game.js:505` `['equip','本命飞剑','feijian',4]` |
| 局内几乎无随机源 | **方向对，措辞不准**：洗牌本身是随机；缺的是**增减益事件层** | 全局无天时/事件牌表 |
| 无引导局 | **准确** | 全库无 `tutorial`/`引导局` 标识 |
| 模块孤岛，唯一闭环是「对战→包袱斋」且偏单向 | **高估**（详见第三节） | 实为 1 条双向主链 + 2 条单向链 |
| 山水祠 48 旬 / 落魄山 54 选点 17 结局 | **准确** | `shenci.js` / `sect.js` |

---

## 三、它最大的判断偏差：模块孤岛被高估

它的一句原文是"模块之间几乎没有真正的经济/成长闭环（仅包袱斋一条单向链）"。**这句话是错的**，项目里至少有 3 条跨模块链：

| 链 | 方向 | 代码证据 |
|---|---|---|
| 对战 ↔ 包袱斋 | **双向**（不是单向） | 出：`game.js:1225` `bfAddCoin(win?BF_COIN_WIN:BF_COIN_LOSE)`；入：`game.js:1250` `bfApplyLoadout()` / `baofuzhai.js:306` |
| 落魄山 → 对战 | 单向供养 | 写：`sect.js:2662` `jianlai_sect_allies`；读：`game.js:1189 loadSectAllies()`（弟子训练带入对战） |
| 包袱斋 → 行迹录 | 单向读数 | `profile.js:312` 读 `jianlai_baofu` 的 `coin` |

**真正缺的不是"连接"，是"不通兑"。** 现状是 4 套互不兑换的资源各算各的：

- 雪花钱（包袱斋）· 香火→功德（山水祠）· 金精铜钱/人情（落魄山）· 声望/XP（行迹录）

没有任何一处能看到"我总共攒了多少家底"。所以"一本账"的价值不在**连模块**（已经连了），在**让已有连接被看见、可兑换**——这意味着 1A 的真实工作量比它估的"中"要**小**，但**前置条件更硬**（见下一节）。

---

## 四、我的独立分析：三件它没看到的事

### 1. 双端参数漂移 —— 未被任何文档提及的结构性风险

PC 与小程序是**两套源码拷贝**（`baofuzhai.js` ×2、`sect.js` ×2、`shenci.js`、`profile.js`、`engine.js`）。数值靠人工同步。`BF_COIN_WIN/BF_COIN_LOSE`、`TIER_LIMIT`、`REALM_LIMIT`、`JP_THRESHOLD` 这类常量，一旦两端不一致，就破了"双端一致"铁律——**而且不会报任何错**，只会让两端的平衡感悄悄分叉。这类漂移在冒烟测试里查不出来。

→ 建议：加一条「双端常量一致性门禁」（抽两端同名常量表比对，不一致即失败）。成本约半天，防的是一整类静默事故。

### 2. 存档迁移必须先于"一本账"，且这是唯一不可逆的一步

它的方案自己标了"风险：中（存档迁移）"，但**没给任何方案**。现状是 8 个互相独立的 `localStorage` key：

`jianlai_record` `jianlai_diff` `jianlai_sect_allies` `jianlai_baofu` `jianlai_profile` `jianlai_shenci` `jianlai_asklake` `ljtq_v1`

而版本字段只有一个零散的 `BF_VER`（`baofuzhai.js:188`）。

统一账本 = 一次性改写 6 个模块的存档结构。**没有全局 `SCHEMA_VERSION` + 迁移函数就动手，会打坏老玩家存档**，且不可逆——这跟"加特效""加天象"这类可回滚的改动不是一个风险等级。

→ 顺序修正：**先加版本与迁移骨架（可测、可回滚）→ 再做账本**。这也让账本能分两步走：先做**只读账目总览**（零迁移风险，验证玩家到底看不看），再决定要不要真正大一统。

### 3. "讲道理"不该做成"暗牌比点数"

它提议"双方暗置一张手牌，比点数 + 道理值"。我的判断是**不要**：

- **与现有机制冲突**：项目里"讲道理"已是**一套完整的 judge 判定链**（`game.js:1456` `JP_THRESHOLD=6`、所有 `judge+v` 技能、以及【问心】【化外】【通晓】【至高】四个直接挂在它上面的角色技能）。再叠一套"暗牌比点数"，等于同一概念两套规则——这是设计债，将来必然打架。
- **原著也不对**：原著"讲道理"是**立规矩、争道统、说服**，不是比大小。做成比大小，恰好把它作为 IP 的分量做没了。

→ **替代方案（改动面小一个量级）**：把 judge 从"被动概率"改成"**主动投入**"——判定时玩家可选择弃一张手牌来"讲道理"提升判定值。收益：现有所有带 `judge+v` 的角色、以及读书/文庙/包袱斋书目，**立刻从"数值"变成"决策"**，且完全复用现成 UI 与判定链。

---

## 五、我认同它的部分

- **小程序不做音效**——我强化这条结论并给出它没给的理由：PC 的 24 条音效是 **WebAudio 实时合成（零资源）**，小程序要等价必须用真音频文件，会**直接顶爆 packageArt（已 86%）**。唯一正确路径是**触觉 + 视觉补偿**，不是补音频文件。
- **不做阵营数值调平** —— 认同，单卡方差 ≠ 阵营失衡。
- **难度质变（AI 错误率/蓄爆，而非只改资源）** —— 认同，这是现有 DIFF 表最大的短板。
- **每条改动必须能回答"制造了什么新决策"** —— 这条应该写进项目纪律，是全篇最有价值的一句。

---

## 六、优先级重排（我的版本）

| 批次 | 内容 | 为什么在这个位置 | 成本 |
|---|---|---|---|
| **1. 收尾** | 真机复验（分包迁移后）+ **双端常量一致性门禁** | 门禁体系已齐，只剩这两件；双端漂移是静默风险 | 半天 |
| **2. 半成品收尾** | 小程序触觉**补齐覆盖**（`vb()` 已存在，不是新建）+ 处决驻留 + 热区下限 + 演出强度开关 | 全部是"改造已有"，不是"开发新功能"，感知强、风险近零 | 3–5 天 |
| **3. 局内变化** | 「**天象**」（即它说的天时）+ 飞剑 subtype | 直接回答"再玩一局"；纯数据表 + 一键可关 | 1–2 周 |
| **4. 角色兑现** | 108 角色技能触发率复核（它排 P2，我认为该提前） | 108 个角色共用一个飞剑、一套判定、一副牌库——**差异只在技能文本里**。招牌技能不触发，108 个角色就只是 108 张立绘 | 1 周 |
| **5. 防雷** | 存档 `SCHEMA_VERSION` + 迁移骨架 → **只读账目总览** | 唯一不可逆的一步，必须前置；且先验证再看要不要大一统 | 1 周 |
| **6. 讲道理** | 按第四节方案改造 judge 为"主动投入" | 复用现有链，成本远低于它的"暗牌比点数" | 1 周 |
| **7. 引导局** | 强制首局引导 | 影响留存的最后一块 | 1 周 |

**与它的顺序差异，就三处**：触觉从"新建"降级为"补齐"并前置；防雷（存档+双端常量）从第三批提前到第一批；角色技能复核从最后提到中间。

---

## 七、一个容易踩的概念坑：别把"天时"当环境牌的名字

原著里「**天时 / 地利 / 人和**」是**十四境合道的三条途径**，`game.js:236` 明载，并且礼圣走的是"特殊合道，不在天时/地利/人和三途径内"。

把它用作"局内环境牌"的名字，会让一个在项目里已有明确所指的原著名词变成另一个意思——违反"概念/命名不可乱套"的既有纪律。

→ 建议改名：**「天象」**（或"此局气象"）。玩法设想照用，只是不占用已有名词。

---

## 八、三句话结论

1. 它说"缺经络、缺心跳"方向是对的，但**经络已经搭了三条**——要做的是让它们**通兑、可见**，不是重新搭。
2. 优先级应重排：**收官门禁 → 触觉/演出补齐 → 天象 + 飞剑 → 全员技能复核 → 存档骨架与账本 → 讲道理 → 引导局**。它把**不可逆**的存档迁移和双端漂移放得太晚。
3. 它最值得采纳的是那条设计原则——**"每条改动必须能回答制造了什么新决策"**；建议连同"不做阵营调平"一起写进项目纪律。
